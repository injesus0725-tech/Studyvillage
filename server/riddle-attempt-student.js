/* v2.1 riddle record enforcement with Korea-day limits and gradual early XP. */
import Database from 'better-sqlite3';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { normalizeAttemptPolicy } from './activity-attempt-policy.js';
import { readActivityAttemptPolicies } from './activity-attempt-settings.js';
import { readExtraAttempts, consumeExtraAttempts } from './activity-attempt-exceptions.js';
import { customTitleView } from './custom-title.js';
import { activityXpReward } from './reward-economy.js';
const __filename=fileURLToPath(import.meta.url),__dirname=path.dirname(__filename),POLICY_ID='riddle-demo';const clean=(v,n=160)=>String(v??'').trim().slice(0,n),clamp=(v,min,max)=>Math.max(min,Math.min(max,Number(v)||0));
function openDb(){const dataDir=process.env.STUDYVILLAGE_DATA_DIR||__dirname,db=new Database(path.join(dataDir,'studyvillage.db'));db.pragma('busy_timeout = 3000');return db}function getSetting(db,key){return db.prepare('SELECT value FROM settings WHERE key=?').get(key)?.value||null}function setSetting(db,key,value){db.prepare('INSERT INTO settings(key,value) VALUES(?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value').run(key,String(value))}
function classroomDayRange(now=new Date()){const parts=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Seoul',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(now),value=t=>parts.find(p=>p.type===t)?.value,start=new Date(`${value('year')}-${value('month')}-${value('day')}T00:00:00+09:00`),end=new Date(start.getTime()+86400000);return{start:start.toISOString(),end:end.toISOString()}}function dailyRiddleAttempts(db,name,range=classroomDayRange()){return Number(db.prepare("SELECT COUNT(*) AS count FROM activity_log WHERE player_name=? AND type='quiz-complete' AND created_at>=? AND created_at<?").get(name,range.start,range.end)?.count)||0}
const xpForLevel=level=>{const n=Math.max(0,(Number(level)||1)-1);return 200*n+25*n*(n-1)},levelFromXp=xp=>{const value=Math.max(0,Number(xp)||0);let level=1;while(value>=xpForLevel(level+1))level++;return level},progressFromXp=xp=>{const value=Math.max(0,Number(xp)||0),level=levelFromXp(value),start=xpForLevel(level),next=xpForLevel(level+1);return{level,xpIntoLevel:value-start,xpToNext:next-start}};
function growthAdjustedXp(_currentXp,baseXp){return Math.max(0,Math.round(Math.max(0,Number(baseXp)||0)))}
const nextGrowthRewardFor=level=>level<2?{level:2,title:'배움 여행자',badge:null}:level<3?{level:3,title:'성장하는 도전자',badge:{icon:'⭐',name:'성장하는 학습자'}}:level<5?{level:5,title:'학습 탐험가',badge:{icon:'🎓',name:'학습 탐험가'}}:level<10?{level:10,title:'숲길 개척자',badge:{icon:'🌲',name:'숲길 개척자'}}:level<20?{level:20,title:'지혜의 길잡이',badge:{icon:'🧭',name:'지혜의 길잡이'}}:level<30?{level:30,title:'별빛 연구자',badge:{icon:'🔭',name:'별빛 연구자'}}:level<40?{level:40,title:'마을 수호자',badge:{icon:'🛡️',name:'마을 수호자'}}:level<50?{level:50,title:'전설의 학습가',badge:{icon:'🌈',name:'전설의 학습가'}}:level<60?{level:60,title:'나만의 칭호',badge:null}:null;const nextNpcUnlockFor=level=>level<10?{level:10,id:'wizard',icon:'🧙',name:'별빛 마법사'}:level<20?{level:20,id:'dragon',icon:'🐲',name:'꼬마 용'}:level<30?{level:30,id:'owl',icon:'🦉',name:'지혜 부엉이'}:null;const NPC_LEVEL_UNLOCKS=Object.freeze([{level:10,id:'wizard',icon:'🧙',name:'별빛 마법사'},{level:20,id:'dragon',icon:'🐲',name:'꼬마 용'},{level:30,id:'owl',icon:'🦉',name:'지혜 부엉이'}]);
function rewardsFor(r){const level=levelFromXp(r.xp),badges=[];if((r.attempts||0)>=1)badges.push({id:'first-challenge',icon:'🌱',name:'첫 도전'});if((r.best_score||0)>=1000)badges.push({id:'perfect',icon:'🏆',name:'수수께끼 마스터'});if((r.attempts||0)>=10)badges.push({id:'persistent',icon:'🔥',name:'꾸준한 도전자'});if((r.login_count||0)>=10)badges.push({id:'regular',icon:'📅',name:'마을 단골'});if(level>=3)badges.push({id:'growing',icon:'⭐',name:'성장하는 학습자'});if(level>=5)badges.push({id:'scholar',icon:'🎓',name:'학습 탐험가'});if(level>=10)badges.push({id:'trailblazer',icon:'🌲',name:'숲길 개척자'});if(level>=20)badges.push({id:'guide',icon:'🧭',name:'지혜의 길잡이'});if(level>=30)badges.push({id:'stargazer',icon:'🔭',name:'별빛 연구자'});if(level>=40)badges.push({id:'guardian',icon:'🛡️',name:'마을 수호자'});if(level>=50)badges.push({id:'legend',icon:'🌈',name:'전설의 학습가'});let title='새싹 주민';if(level>=2)title='배움 여행자';if(level>=3)title='성장하는 도전자';if(level>=5)title='학습 탐험가';if(level>=10)title='숲길 개척자';if(level>=20)title='지혜의 길잡이';if(level>=30)title='별빛 연구자';if(level>=40)title='마을 수호자';if(level>=50)title='전설의 학습가';if((r.best_score||0)>=1000)title='수수께끼 마스터';return{title,badges}}
function playerView(db,r){const rewards=rewardsFor(r),progress=progressFromXp(r.xp),custom=customTitleView(key=>getSetting(db,key),r.name,progress.level);return{name:r.name,totalScore:r.total_score,attempts:r.attempts,bestScore:r.best_score,lastScore:r.last_score,loginCount:r.login_count||0,lastLoginAt:r.last_login_at||null,xp:r.xp||0,...progress,title:progress.level>=60&&custom.customTitle?custom.customTitle:rewards.title,badges:rewards.badges,nextGrowthReward:nextGrowthRewardFor(progress.level),nextNpcUnlock:nextNpcUnlockFor(progress.level),...custom,updatedAt:r.updated_at}}function logActivity(db,name,type,detail){db.prepare('INSERT INTO activity_log(player_name,type,detail,created_at) VALUES(?,?,?,?)').run(name,type,detail,new Date().toISOString())}
export function installRiddleAttemptStudentRoutes(app,{requireSession,publishLiveEvent,commitRiddleReward}){app.post('/api/player/me/record',requireSession,(req,res)=>{let db;try{db=openDb();const name=req.session.name,current=db.prepare('SELECT * FROM players WHERE name=?').get(name);if(!current)return res.status(404).json({ok:false,code:'player-not-found'});const incoming={totalScore:clamp(req.body?.totalScore,0,100000000),attempts:clamp(req.body?.attempts,0,1000000),bestScore:clamp(req.body?.bestScore,0,1000),lastScore:clamp(req.body?.lastScore,0,1000)},newAttempts=Math.max(0,incoming.attempts-current.attempts),policies=readActivityAttemptPolicies(key=>getSetting(db,key)),policy=normalizeAttemptPolicy(policies[POLICY_ID]||{}),daily=policy.period==='daily',periodAttempts=daily?dailyRiddleAttempts(db,name):Number(current.attempts||0),unlimited=policy.mode==='unlimited',baseRemaining=unlimited?Number.POSITIVE_INFINITY:Math.max(0,Number(policy.limit||1)-periodAttempts),extraBefore=readExtraAttempts(key=>getSetting(db,key),name,POLICY_ID),extraNeeded=newAttempts?Math.max(0,newAttempts-(Number.isFinite(baseRemaining)?baseRemaining:newAttempts)):0;if(extraNeeded>extraBefore)return res.status(409).json({ok:false,code:'attempt-limit-reached',activityId:POLICY_ID,attempts:periodAttempts,remaining:Number.isFinite(baseRemaining)?baseRemaining:null,extraAttempts:extraBefore,policy});const tx=db.transaction(()=>{const latest=db.prepare('SELECT * FROM players WHERE name=?').get(name);if(!latest)throw Object.assign(new Error('player-not-found'),{code:'player-not-found'});if(latest.attempts!==current.attempts)throw Object.assign(new Error('record-changed'),{code:'record-changed'});const latestPeriodAttempts=daily?dailyRiddleAttempts(db,name):Number(latest.attempts||0),latestBaseRemaining=unlimited?Number.POSITIVE_INFINITY:Math.max(0,Number(policy.limit||1)-latestPeriodAttempts),latestExtra=readExtraAttempts(key=>getSetting(db,key),name,POLICY_ID),latestExtraNeeded=newAttempts?Math.max(0,newAttempts-(Number.isFinite(latestBaseRemaining)?latestBaseRemaining:newAttempts)):0;if(latestExtraNeeded>latestExtra)throw Object.assign(new Error('attempt-limit-reached'),{code:'attempt-limit-reached'});if(latestExtraNeeded){const consumed=consumeExtraAttempts(key=>getSetting(db,key),(key,value)=>setSetting(db,key,value),name,POLICY_ID,latestExtraNeeded,'수수께끼 추가 도전 사용');if(!consumed.ok)throw Object.assign(new Error(consumed.code),{code:consumed.code})}const awardXp=newAttempts>0&&(policy.xpMode==='every-attempt'||latestPeriodAttempts===0),baseXp=activityXpReward('riddle',incoming.lastScore),gained=awardXp?growthAdjustedXp(latest.xp,baseXp):0,now=new Date().toISOString(),nextTotal=Math.max(Number(latest.total_score)||0,incoming.totalScore),nextBest=Math.max(Number(latest.best_score)||0,incoming.bestScore),nextAttempts=Math.max(Number(latest.attempts)||0,incoming.attempts),nextLast=newAttempts>0?incoming.lastScore:Number(latest.last_score)||0;db.prepare('UPDATE players SET total_score=?,attempts=?,best_score=?,last_score=?,xp=xp+?,updated_at=? WHERE name=?').run(nextTotal,nextAttempts,nextBest,nextLast,gained,now,name);if(newAttempts>0)logActivity(db,name,'quiz-complete',`${incoming.lastScore}점${gained?` · +${gained}XP`:' · XP 추가 없음'}${latestExtraNeeded?` · 추가 도전권 ${latestExtraNeeded}회 사용`:''}${newAttempts>1?' · 레거시 묶음 기록':''}`);const starReward=newAttempts>0&&commitRiddleReward?commitRiddleReward(db,{name,attempt:nextAttempts,score:incoming.lastScore,now}):{stars:0,balance:null},updated=db.prepare('SELECT * FROM players WHERE name=?').get(name),oldLevel=levelFromXp(current.xp),newLevel=levelFromXp(updated.xp),npcUnlocks=NPC_LEVEL_UNLOCKS.filter(npc=>oldLevel<npc.level&&newLevel>=npc.level);if(newLevel>oldLevel)logActivity(db,name,'level-up',`Lv.${newLevel} 달성`);for(const npcUnlock of npcUnlocks)logActivity(db,name,'npc-unlock',`Lv.${npcUnlock.level} · ${npcUnlock.name}`);return{player:playerView(db,updated),gainedXp:gained,riddleStars:starReward.stars,starBalance:starReward.balance,extraAttemptsUsed:latestExtraNeeded,extraAttemptsRemaining:Math.max(0,latestExtra-latestExtraNeeded),periodAttempts:latestPeriodAttempts+newAttempts,resetAt:daily?classroomDayRange().end:null,liveLevelUp:newLevel>oldLevel?newLevel:null,liveNpcUnlocks:npcUnlocks}});const result=tx(),{liveLevelUp,liveNpcUnlocks,...response}=result;try{if(liveLevelUp)publishLiveEvent?.('🎉',`${name} 학생이 Lv.${liveLevelUp}에 올랐습니다!`,'level-up');for(const npcUnlock of liveNpcUnlocks)publishLiveEvent?.(npcUnlock.icon,`${name} 학생이 새 마을 친구 “${npcUnlock.name}”을 만날 수 있게 되었습니다!`,'npc-unlock')}catch{}res.json({ok:true,...response})}catch(err){const code=clean(err?.code||'riddle-record-save-failed',80);res.status(code==='attempt-limit-reached'||code==='record-changed'?409:500).json({ok:false,code,message:clean(err?.message||err,160)})}finally{try{db?.close()}catch{}}})}
