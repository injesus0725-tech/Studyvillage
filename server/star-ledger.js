/* v1.16 star currency foundation. */
import Database from 'better-sqlite3';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { installItemShopRoutes } from './item-shop.js';
import { installActivityAttemptStudentRoutes } from './activity-attempt-student.js';
import { standardActivityStars } from './reward-economy.js';
import { installRiddleAttemptStudentRoutes } from './riddle-attempt-student.js';
import { installStudentScoreHistoryRoutes } from './student-score-history.js';
import { installRestoreValidationMiddleware } from './restore-validation-middleware.js';
import { installMathPracticeRoutes, validateMathCompletion, finalizeMathCompletion } from './math-practice.js';

const __filename=fileURLToPath(import.meta.url),__dirname=path.dirname(__filename);
const MAX_STARS=1000000,MAX_MIRROR_ENTRIES=500;
const clean=(v,n=160)=>String(v??'').trim().slice(0,n);
const mirrorKey=name=>`compat:stars:${encodeURIComponent(clean(name,12))}`;
const validStarBalance=value=>Number.isSafeInteger(value)&&value>=0&&value<=MAX_STARS;
function openLiveDb(){const dataDir=process.env.STUDYVILLAGE_DATA_DIR||__dirname;const db=new Database(path.join(dataDir,'studyvillage.db'));db.pragma('busy_timeout = 3000');return db}
function ensureSchema(db){const columns=db.prepare('PRAGMA table_info(players)').all().map(r=>r.name);if(!columns.includes('stars'))db.exec('ALTER TABLE players ADD COLUMN stars INTEGER NOT NULL DEFAULT 0');db.exec(`CREATE TABLE IF NOT EXISTS star_ledger (id INTEGER PRIMARY KEY AUTOINCREMENT,player_name TEXT NOT NULL,before_value INTEGER NOT NULL,after_value INTEGER NOT NULL,delta INTEGER NOT NULL,kind TEXT NOT NULL,reference_id TEXT,detail TEXT,created_at TEXT NOT NULL);CREATE INDEX IF NOT EXISTS idx_star_ledger_player_id ON star_ledger(player_name,id DESC);`)}
function readMirror(db,name){const raw=db.prepare('SELECT value FROM settings WHERE key=?').get(mirrorKey(name))?.value;try{const value=JSON.parse(raw||'null'),balance=Number(value?.balance),entries=Array.isArray(value?.entries)?value.entries:[];if(!Number.isInteger(balance)||balance<0||balance>MAX_STARS)return null;return{balance,entries:entries.slice(-MAX_MIRROR_ENTRIES)}}catch{return null}}
function liveSnapshot(db,name){const row=db.prepare('SELECT stars FROM players WHERE name=?').get(name);if(!row)return null;return{balance:row.stars,entries:db.prepare(`SELECT before_value AS beforeValue,after_value AS afterValue,delta,kind,reference_id AS referenceId,detail,created_at AS createdAt FROM star_ledger WHERE player_name=? ORDER BY id ASC LIMIT ?`).all(name,MAX_MIRROR_ENTRIES)}}
function normalized(value){return JSON.stringify({balance:Number(value?.balance)||0,entries:(value?.entries||[]).map(e=>({beforeValue:Number(e?.beforeValue)||0,afterValue:Number(e?.afterValue)||0,delta:Number(e?.delta)||0,kind:clean(e?.kind,60)||'legacy',referenceId:clean(e?.referenceId,100)||null,detail:clean(e?.detail,240)||null,createdAt:clean(e?.createdAt,80)}))})}
function writeMirror(db,name){const snap=liveSnapshot(db,name);if(!snap)return;if(!validStarBalance(snap.balance))throw new Error('corrupt-star-balance');db.prepare(`INSERT INTO settings(key,value) VALUES(?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value`).run(mirrorKey(name),normalized(snap))}
function recoverFromMirror(db,name){const mirror=readMirror(db,name);if(!mirror)return false;const live=liveSnapshot(db,name);if(!live||validStarBalance(live.balance)&&normalized(live)===normalized(mirror))return false;db.transaction(()=>{db.prepare('UPDATE players SET stars=?,updated_at=? WHERE name=?').run(mirror.balance,new Date().toISOString(),name);db.prepare('DELETE FROM star_ledger WHERE player_name=?').run(name);const insert=db.prepare('INSERT INTO star_ledger(player_name,before_value,after_value,delta,kind,reference_id,detail,created_at) VALUES(?,?,?,?,?,?,?,?)');for(const e of mirror.entries)insert.run(name,e.beforeValue,e.afterValue,e.delta,e.kind,e.referenceId,e.detail,e.createdAt||new Date().toISOString())})();return true}
const EXPEDITION_REWARD_IDS=new Set(['exploration-forest-riddle','exploration-mountain-riddle','exploration-korean','exploration-math','exploration-social','exploration-science','exploration-random']),STANDARD_REWARD_IDS=new Set(['math-arithmetic','vocabulary','curriculum-korean','curriculum-math','curriculum-social','curriculum-science','curriculum-arts']);
function expeditionStarsFor(activityId,score){const value=Math.max(0,Math.min(100,Math.round(Number(score)||0)));return activityId.startsWith('exploration-')&&value>0?Math.min(2,Math.ceil(value/50)):0}
function commitExpeditionReward(db,{name,activityId,score,submissionId,now,starDelta=0}){if(!EXPEDITION_REWARD_IDS.has(activityId)&&!STANDARD_REWARD_IDS.has(activityId))return{stars:0,balance:null};if(!/^[A-Za-z0-9._:-]{8,100}$/.test(submissionId||''))throw Object.assign(new Error('invalid-expedition-submission'),{code:'invalid-expedition-submission'});ensureSchema(db);const kind=EXPEDITION_REWARD_IDS.has(activityId)?'expedition-completion':'learning-completion',prior=db.prepare('SELECT after_value AS balance FROM star_ledger WHERE player_name=? AND kind=? AND reference_id=? LIMIT 1').get(name,kind,submissionId);if(prior)return{stars:0,balance:prior.balance,alreadyClaimed:true};const player=db.prepare('SELECT stars FROM players WHERE name=?').get(name);if(!player)throw Object.assign(new Error('player-not-found'),{code:'player-not-found'});const baseStars=EXPEDITION_REWARD_IDS.has(activityId)?expeditionStarsFor(activityId,score):standardActivityStars(activityId,score),npcDelta=Math.max(-5,Math.min(8,Math.trunc(Number(starDelta)||0))),before=player.stars,stars=npcDelta<0?-Math.min(before,Math.abs(npcDelta)):baseStars+npcDelta,after=before+stars;if(after>MAX_STARS)throw new Error('star-limit-exceeded');if(stars){db.prepare('UPDATE players SET stars=?,updated_at=? WHERE name=?').run(after,now,name);db.prepare('INSERT INTO star_ledger(player_name,before_value,after_value,delta,kind,reference_id,detail,created_at) VALUES(?,?,?,?,?,?,?,?)').run(name,before,after,stars,kind,submissionId,`${activityId} 정상 완료 · ${Math.round(Number(score)||0)}점`,now);writeMirror(db,name)}return{stars,balance:after,alreadyClaimed:false}}
function riddleStarsFor(score){const value=Math.max(0,Math.min(1000,Math.round(Number(score)||0)));return value>=1000?3:value>=700?2:value>0?1:0}
function commitRiddleReward(db,{name,attempt,score,now}){ensureSchema(db);const referenceId=`riddle-demo:${Math.max(1,Number(attempt)||1)}`,prior=db.prepare("SELECT after_value AS balance FROM star_ledger WHERE player_name=? AND kind='riddle-completion' AND reference_id=? LIMIT 1").get(name,referenceId);if(prior)return{stars:0,balance:prior.balance,alreadyClaimed:true};const player=db.prepare('SELECT stars FROM players WHERE name=?').get(name);if(!player)throw new Error('player-not-found');const stars=riddleStarsFor(score),before=player.stars,after=before+stars;if(stars){db.prepare('UPDATE players SET stars=?,updated_at=? WHERE name=?').run(after,now,name);db.prepare('INSERT INTO star_ledger(player_name,before_value,after_value,delta,kind,reference_id,detail,created_at) VALUES(?,?,?,?,?,?,?,?)').run(name,before,after,stars,'riddle-completion',referenceId,`기본 수수께끼 정상 완료 · ${Math.round(Number(score)||0)}점`,now);writeMirror(db,name)}return{stars,balance:after,alreadyClaimed:false}}
export function ensureStarLedger(){let db;try{db=openLiveDb();ensureSchema(db);return true}finally{try{db?.close()}catch{}}}
export function starBalanceFor(playerName){let db;try{db=openLiveDb();ensureSchema(db);const name=clean(playerName,12);recoverFromMirror(db,name);return db.prepare('SELECT stars FROM players WHERE name=?').get(name)?.stars??null}finally{try{db?.close()}catch{}}}
export function starLedgerFor(playerName,{limit=100}={}){let db;try{db=openLiveDb();ensureSchema(db);const name=clean(playerName,12);recoverFromMirror(db,name);return db.prepare(`SELECT id,before_value AS beforeValue,after_value AS afterValue,delta,kind,reference_id AS referenceId,detail,created_at AS createdAt FROM star_ledger WHERE player_name=? ORDER BY id DESC LIMIT ?`).all(name,Math.max(1,Math.min(300,Number(limit)||100)))}finally{try{db?.close()}catch{}}}
export function changeStars(playerName,delta,{kind='teacher-adjustment',referenceId='',detail=''}={}){const name=clean(playerName,12),change=Number(delta);if(!name||!Number.isInteger(change)||change===0||Math.abs(change)>MAX_STARS)return{ok:false,code:'invalid-star-change'};let db;try{db=openLiveDb();ensureSchema(db);recoverFromMirror(db,name);return db.transaction(()=>{const player=db.prepare('SELECT stars FROM players WHERE name=?').get(name);if(!player)return{ok:false,code:'player-not-found'};const before=player.stars,after=before+change;if(after<0)return{ok:false,code:'insufficient-stars',balance:before};if(after>MAX_STARS)return{ok:false,code:'star-limit-exceeded',balance:before};const now=new Date().toISOString();db.prepare('UPDATE players SET stars=?,updated_at=? WHERE name=?').run(after,now,name);const result=db.prepare('INSERT INTO star_ledger(player_name,before_value,after_value,delta,kind,reference_id,detail,created_at) VALUES(?,?,?,?,?,?,?,?)').run(name,before,after,change,clean(kind,60),clean(referenceId,100)||null,clean(detail,240)||null,now);writeMirror(db,name);return{ok:true,id:Number(result.lastInsertRowid),beforeValue:before,afterValue:after,delta:change,createdAt:now}})()}finally{try{db?.close()}catch{}}}
const EXPLORATION_REWARDS=Object.freeze({chest:{stars:3,detail:'보물상자 발견'},tree:{stars:1,detail:'반짝 나무 관찰'},flower:{stars:2,detail:'숨은 꽃 피우기'}}),COLLECTION_NPCS=new Set(['wizard','ghost','dragon','fox','robot','owl']),COLLECTION_EVENTS=new Set(Object.keys(EXPLORATION_REWARDS));
const collectionKey=name=>`exploration-collection:${encodeURIComponent(clean(name,12))}`;
function readCollection(db,name){const raw=db.prepare('SELECT value FROM settings WHERE key=?').get(collectionKey(name))?.value;try{const value=JSON.parse(raw||'{}');return{npcs:[...new Set((Array.isArray(value.npcs)?value.npcs:[]).filter(id=>COLLECTION_NPCS.has(id)))],events:[...new Set((Array.isArray(value.events)?value.events:[]).filter(id=>COLLECTION_EVENTS.has(id)))]}}catch{return{npcs:[],events:[]}}}
function addCollection(db,name,kind,id){const allowed=kind==='npc'?COLLECTION_NPCS:kind==='event'?COLLECTION_EVENTS:null;if(!allowed?.has(id))return{ok:false,code:'invalid-collection-entry'};const current=readCollection(db,name),key=kind==='npc'?'npcs':'events',isNew=!current[key].includes(id);if(isNew)current[key].push(id);db.prepare('INSERT INTO settings(key,value) VALUES(?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value').run(collectionKey(name),JSON.stringify(current));return{ok:true,isNew,collection:current}}
export function explorationCollectionFor(playerName){const name=clean(playerName,12);if(!name)return null;let db;try{db=openLiveDb();const player=db.prepare('SELECT stars FROM players WHERE name=?').get(name);if(!player)return null;return{ok:true,...readCollection(db,name),balance:player.stars}}finally{try{db?.close()}catch{}}}
export function recordExplorationCollection(playerName,kind,id){const name=clean(playerName,12);let db;try{db=openLiveDb();if(!db.prepare('SELECT 1 FROM players WHERE name=?').get(name))return{ok:false,code:'player-not-found'};return addCollection(db,name,clean(kind,12),clean(id,30))}finally{try{db?.close()}catch{}}}
export function classroomExplorationCollections(){let db;try{db=openLiveDb();return db.prepare('SELECT name FROM players ORDER BY name COLLATE NOCASE').all().map(({name})=>({name,...readCollection(db,name)}))}finally{try{db?.close()}catch{}}}
const DAILY_MISSIONS=Object.freeze([
 {id:'bookmaru',activityIds:['vocabulary'],icon:'📚',title:'책마루 완료',detail:'책마루 활동을 1회 완료해요.'},
 {id:'math',activityIds:['math-arithmetic'],icon:'➕',title:'수학 놀이터 완료',detail:'수학 놀이터에서 랜덤 복습을 1회 완료해요.'},
 {id:'exploration',activityIds:['exploration-korean','exploration-math','exploration-social','exploration-science','exploration-random'],icon:'🗺️',title:'탐험 완료',detail:'탐험 동굴에서 원하는 탐험을 1회 완료해요.'}
]),DAILY_REWARD=3;
const classroomDay=()=>new Date().toLocaleDateString('en-CA',{timeZone:'Asia/Seoul'}),kstDay=value=>{const date=new Date(value);return Number.isNaN(date.getTime())?'':date.toLocaleDateString('en-CA',{timeZone:'Asia/Seoul'})};
function missionProgress(db,name,day){return DAILY_MISSIONS.map(mission=>{const placeholders=mission.activityIds.map(()=>'?').join(','),rows=db.prepare(`SELECT activity_id AS activityId,updated_at AS updatedAt FROM activity_records WHERE player_name=? AND activity_id IN (${placeholders})`).all(name,...mission.activityIds),complete=rows.some(row=>kstDay(row.updatedAt)===day);return{...mission,complete}})}
export function dailyMissionStatus(playerName){const name=clean(playerName,12);if(!name)return null;let db;try{db=openLiveDb();ensureSchema(db);recoverFromMirror(db,name);const player=db.prepare('SELECT stars FROM players WHERE name=?').get(name);if(!player)return null;const day=classroomDay(),missions=missionProgress(db,name,day),completed=missions.filter(m=>m.complete).length,complete=completed===missions.length,referenceId=`daily-mission:${day}`,claimed=!!db.prepare("SELECT 1 FROM star_ledger WHERE player_name=? AND kind='daily-mission' AND reference_id=? LIMIT 1").get(name,referenceId);return{ok:true,day,missions,completed,total:missions.length,complete,claimed,rewardStars:DAILY_REWARD,balance:player.stars}}finally{try{db?.close()}catch{}}}
export function claimDailyMission(playerName){const name=clean(playerName,12);if(!name)return{ok:false,code:'invalid-player'};let db;try{db=openLiveDb();ensureSchema(db);recoverFromMirror(db,name);const day=classroomDay(),referenceId=`daily-mission:${day}`;return db.transaction(()=>{const player=db.prepare('SELECT stars FROM players WHERE name=?').get(name);if(!player)return{ok:false,code:'player-not-found'};const prior=db.prepare("SELECT after_value AS balance FROM star_ledger WHERE player_name=? AND kind='daily-mission' AND reference_id=? LIMIT 1").get(name,referenceId);if(prior)return{ok:true,alreadyClaimed:true,...dailyMissionStatus(name),stars:0,balance:prior.balance};const missions=missionProgress(db,name,day),completed=missions.filter(m=>m.complete).length;if(completed!==missions.length)return{ok:false,code:'mission-not-complete',day,missions,completed,total:missions.length,complete:false,claimed:false,rewardStars:DAILY_REWARD,balance:player.stars};const before=player.stars,after=before+DAILY_REWARD,now=new Date().toISOString();db.prepare('UPDATE players SET stars=?,updated_at=? WHERE name=?').run(after,now,name);db.prepare('INSERT INTO star_ledger(player_name,before_value,after_value,delta,kind,reference_id,detail,created_at) VALUES(?,?,?,?,?,?,?,?)').run(name,before,after,DAILY_REWARD,'daily-mission',referenceId,'오늘의 의뢰 3개 모두 완료',now);writeMirror(db,name);return{ok:true,alreadyClaimed:false,day,missions,completed,total:missions.length,complete:true,claimed:true,rewardStars:DAILY_REWARD,stars:DAILY_REWARD,balance:after,createdAt:now}})()}finally{try{db?.close()}catch{}}}
export function claimExplorationReward(playerName,eventType){const name=clean(playerName,12),type=clean(eventType,20),reward=EXPLORATION_REWARDS[type];if(!name||!reward)return{ok:false,code:'invalid-exploration-event'};let db;try{db=openLiveDb();ensureSchema(db);recoverFromMirror(db,name);const day=classroomDay(),referenceId=`${type}:${day}`;return db.transaction(()=>{const player=db.prepare('SELECT stars FROM players WHERE name=?').get(name);if(!player)return{ok:false,code:'player-not-found'};const prior=db.prepare("SELECT after_value AS balance FROM star_ledger WHERE player_name=? AND kind='exploration-event' AND reference_id=? LIMIT 1").get(name,referenceId);if(prior){addCollection(db,name,'event',type);return{ok:true,alreadyClaimed:true,eventType:type,stars:0,balance:prior.balance}}const before=player.stars,after=before+reward.stars,now=new Date().toISOString();db.prepare('UPDATE players SET stars=?,updated_at=? WHERE name=?').run(after,now,name);db.prepare('INSERT INTO star_ledger(player_name,before_value,after_value,delta,kind,reference_id,detail,created_at) VALUES(?,?,?,?,?,?,?,?)').run(name,before,after,reward.stars,'exploration-event',referenceId,reward.detail,now);addCollection(db,name,'event',type);writeMirror(db,name);return{ok:true,alreadyClaimed:false,eventType:type,stars:reward.stars,balance:after,createdAt:now}})()}finally{try{db?.close()}catch{}}}
export function installStarLedgerRoutes(app,{requireSession,requireAdmin,publishLiveEvent}){ensureStarLedger();installRestoreValidationMiddleware(app,{requireAdmin});installStudentScoreHistoryRoutes(app,{requireSession});app.post('/api/login',(req,res,next)=>{const name=String(req.body?.name??'').trim().replace(/\s+/g,' ');if(name.length>12)return res.status(400).json({ok:false,code:'invalid-input'});next()});app.post('/api/player/me/record',(req,res,next)=>{const clamp=(v,min,max)=>Math.max(min,Math.min(max,Number(v)||0)),totalScore=clamp(req.body?.totalScore,0,100000000),attempts=clamp(req.body?.attempts,0,1000000),bestScore=clamp(req.body?.bestScore,0,1000),lastScore=clamp(req.body?.lastScore,0,1000);if(attempts===0&&(totalScore||bestScore||lastScore)||attempts>0&&(bestScore<lastScore||totalScore<bestScore||totalScore>attempts*1000))return res.status(400).json({ok:false,code:'invalid-record-relationship'});next()});app.get('/api/player/me/stars',requireSession,(req,res)=>{try{const balance=starBalanceFor(req.session.name),entries=starLedgerFor(req.session.name,{limit:req.query.limit});if(balance===null)return res.status(404).json({ok:false,code:'player-not-found'});res.json({ok:true,balance,entries})}catch(err){res.status(500).json({ok:false,code:'star-ledger-read-failed',message:clean(err?.message||err,160)})}});app.post('/api/player/me/exploration-event',requireSession,(req,res)=>{try{const result=claimExplorationReward(req.session.name,req.body?.eventType);res.status(result.ok?200:400).json(result)}catch(err){res.status(500).json({ok:false,code:'exploration-event-failed',message:clean(err?.message||err,160)})}});app.get('/api/player/me/exploration-collection',requireSession,(req,res)=>{try{const result=explorationCollectionFor(req.session.name);if(!result)return res.status(404).json({ok:false,code:'player-not-found'});res.json(result)}catch(err){res.status(500).json({ok:false,code:'exploration-collection-read-failed',message:clean(err?.message||err,160)})}});app.post('/api/player/me/exploration-collection',requireSession,(req,res)=>{try{const result=recordExplorationCollection(req.session.name,req.body?.kind,req.body?.id);res.status(result.ok?200:400).json(result)}catch(err){res.status(500).json({ok:false,code:'exploration-collection-write-failed',message:clean(err?.message||err,160)})}});app.get('/api/admin/exploration-collections',requireAdmin,(_req,res)=>{try{res.json({ok:true,students:classroomExplorationCollections()})}catch(err){res.status(500).json({ok:false,code:'exploration-collections-read-failed',message:clean(err?.message||err,160)})}});app.get('/api/player/me/daily-mission',requireSession,(req,res)=>{try{const result=dailyMissionStatus(req.session.name);if(!result)return res.status(404).json({ok:false,code:'player-not-found'});res.json(result)}catch(err){res.status(500).json({ok:false,code:'daily-mission-read-failed',message:clean(err?.message||err,160)})}});app.post('/api/player/me/daily-mission/claim',requireSession,(req,res)=>{try{const result=claimDailyMission(req.session.name);res.status(result.ok?200:409).json(result)}catch(err){res.status(500).json({ok:false,code:'daily-mission-claim-failed',message:clean(err?.message||err,160)})}});installRiddleAttemptStudentRoutes(app,{requireSession,publishLiveEvent,commitRiddleReward});installMathPracticeRoutes(app,{requireSession});installActivityAttemptStudentRoutes(app,{requireSession,commitExpeditionReward,validateActivityCompletion:validateMathCompletion,finalizeActivityCompletion:finalizeMathCompletion});installItemShopRoutes(app,{requireSession,requireAdmin,publishLiveEvent})}
