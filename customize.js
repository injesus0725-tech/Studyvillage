(()=>{
  const $=s=>document.querySelector(s),button=$('#customize-button'),panel=$('#customize-panel'),close=$('#customize-close'),save=$('#customize-save'),list=$('#inventory-list'),baseList=$('#base-character-list'),message=$('#customize-message'),game=$('#game-screen');
  if(!button||!panel||!save)return;
  const slots=['hair','hat','glasses','outfit','bottom','shoes','bag','hand','pet'],slotNames={hair:'머리카락',hat:'모자',glasses:'안경',outfit:'상의',bottom:'하의',shoes:'신발',bag:'가방',hand:'손 아이템',pet:'친구'},icons={'cap-blue':'🧢','crown-gold':'👑','glasses-round':'👓','backpack':'🎒','pet-chick':'🐣','pet-cat':'🐱','leaf-cap':'🍃','scholar-cap':'🎓','explorer-goggles':'🥽','star-monocle':'🔭','field-satchel':'🧰','book-pack':'📚','pet-owl':'🦉','pet-fox':'🦊'},REQUEST_TIMEOUT_MS=5000,POLL_MS=3500;
  let playerData=null,shopData={ownedItems:[],equipment:{},items:[]},draft={hair:null,hat:null,glasses:null,outfit:null,bottom:null,shoes:null,bag:null,hand:null,pet:null},draftBase='student-boy',polling=false,loadPromise=null,pollTimer=null;
  const headers=()=>window.StudyVillageAuth?.authHeaders?.()||{};
  async function timedFetch(url,options={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),REQUEST_TIMEOUT_MS);try{return await fetch(url,{...options,signal:controller.signal})}finally{clearTimeout(timer)}}
  const ownedSet=()=>new Set(Array.isArray(shopData?.ownedItems)?shopData.ownedItems:[]);
  const builtInItem=id=>(playerData?.inventory||[]).find(item=>item.id===id)||null;
  const shopItem=id=>(shopData?.items||[]).find(item=>item.id===id)||null;
  function itemInfo(id){if(!id)return null;const built=builtInItem(id),shop=shopItem(id);return{id,slot:built?.slot||shop?.slot,icon:built?.icon||icons[id]||'🎁',name:built?.name||shop?.name||id,condition:built?.condition||'',builtUnlocked:built?.unlocked===true,owned:ownedSet().has(id)}}
  function usable(id){const item=itemInfo(id);return !!item&&(item.builtUnlocked||item.owned)}
  function normalizeEquipment(raw={}){const out={hair:null,hat:null,glasses:null,outfit:null,bottom:null,shoes:null,bag:null,hand:null,pet:null};for(const slot of slots){const id=raw?.[slot];if(id&&itemInfo(id)?.slot===slot&&usable(id))out[slot]=id}return out}
  function renderAvatar(){const renderer=window.StudyVillageAvatar;renderer?.paintAvatarBase(document.querySelector('.player-icon'),draftBase);renderer?.paintAvatarBase(document.querySelector('.preview-base'),draftBase);for(const slot of slots){const info=itemInfo(draft[slot]);if(slot==='hair'){renderer?.paintHair(document.querySelector('#player-hair'),info?.id||null,draftBase);renderer?.paintHair(document.querySelector('#preview-hair'),info?.id||null,draftBase);continue}renderer?.paintItem(document.querySelector(`#player-${slot}`),info?.id||null,info?.icon||'');renderer?.paintItem(document.querySelector(`#preview-${slot}`),info?.id||null,info?.icon||'')}}
  function renderBases(){if(!baseList)return;baseList.innerHTML='';for(const c of playerData?.baseCharacters||[]){if(!['student-boy','student-girl'].includes(c.id))continue;const b=document.createElement('button');b.type='button';b.className=`inventory-item ${draftBase===c.id?'selected':''}`;b.innerHTML=`<span class="base-choice-preview"></span><strong>${c.id==='student-girl'?'여자 캐릭터':'남자 캐릭터'}</strong><small>같은 미니미 체형</small>`;window.StudyVillageAvatar?.paintBase(b.querySelector('span'),c.id);b.onclick=()=>{draftBase=c.id;renderBases();renderInventory();renderAvatar()};baseList.appendChild(b)}}
  function allItemsForSlot(slot){const map=new Map();for(const item of playerData?.inventory||[])if(item.slot===slot)map.set(item.id,itemInfo(item.id));for(const item of shopData?.items||[])if(item.slot===slot)map.set(item.id,itemInfo(item.id));return[...map.values()].filter(Boolean)}
  function paintOwnedPreview(button,info,available){if(!available||!window.StudyVillageAvatar?.ASSETS?.[info.id])return;const host=button.querySelector('span');if(!host)return;host.className='item-part-preview';host.replaceChildren();const base=document.createElement('i'),part=document.createElement('i'),renderer=window.StudyVillageAvatar;base.className='shop-preview-base';part.className=`shop-preview-part shop-preview-${info.slot}`;host.append(base);renderer.paintAvatarBase(base,draftBase);if(info.slot!=='hair'){const hair=document.createElement('i');hair.className='shop-preview-hair';host.append(hair);renderer.paintHair(hair,draft.hair,draftBase)}host.append(part);if(info.slot==='hair')renderer.paintHair(part,info.id,draftBase);else renderer.paintItem(part,info.id)}
  function renderInventory(){if(!list)return;list.innerHTML='';for(const slot of slots){const section=document.createElement('section');section.className='inventory-group';section.innerHTML=`<h3>${slotNames[slot]}</h3>`;const items=document.createElement('div');items.className='inventory-items';const none=document.createElement('button');none.type='button';none.className=`inventory-item ${!draft[slot]?'selected':''}`;none.innerHTML='<span>❌</span><strong>착용 안 함</strong>';none.onclick=()=>{draft[slot]=null;renderInventory();renderAvatar()};items.appendChild(none);for(const info of allItemsForSlot(slot)){const available=info.builtUnlocked||info.owned,b=document.createElement('button');b.type='button';b.disabled=!available;b.className=`inventory-item ${draft[slot]===info.id?'selected':''} ${available?'':'locked'}`;const note=info.owned?'구매 완료 · 사용 가능':info.builtUnlocked?'레벨 보상 · 사용 가능':info.condition||'상점에서 구매하면 사용할 수 있어요';b.innerHTML=`<span>${available?info.icon:'🔒'}</span><strong>${info.name}</strong><small>${note}</small>`;if(available)b.onclick=()=>{draft[slot]=info.id;renderInventory();renderAvatar()};paintOwnedPreview(b,info,available);items.appendChild(b)}section.appendChild(items);list.appendChild(section)}}
  async function fetchPlayer(){const r=await timedFetch('/api/player/me',{headers:headers(),cache:'no-store'}),d=await r.json().catch(()=>({}));if(!r.ok||!d.player)throw new Error('player-load-failed');return d.player}
  async function fetchShop(){const r=await timedFetch('/api/shop',{headers:headers(),cache:'no-store'}),d=await r.json().catch(()=>({}));if(!r.ok||!d.ok)throw new Error('shop-load-failed');return d}
  async function load(){if(loadPromise)return loadPromise;loadPromise=(async()=>{try{const[next,shop]=await Promise.all([fetchPlayer(),fetchShop()]);playerData=next;shopData=shop;draftBase=(next.baseCharacters||[]).some(c=>c.id===next.baseCharacter)&&next.baseCharacter==='student-girl'?'student-girl':'student-boy';draft=normalizeEquipment({...next.equipment,...shop.equipment});renderBases();renderInventory();renderAvatar();return true}catch{message.textContent='꾸미기 정보를 불러오지 못했어요.';return false}})();try{return await loadPromise}finally{loadPromise=null}}
  async function openPanel(){if(await load()){panel.hidden=false;message.textContent='머리·상의·하의·신발·가방·손 아이템·친구를 함께 골라 저장할 수 있어요.'}}
  function closePanel(){if(save.disabled)return;panel.hidden=true}
  async function saveEquipment({closeAfter=false}={}){if(save.disabled)return false;save.disabled=true;message.textContent='저장 중...';try{const owned=ownedSet(),legacy={hat:null,glasses:null,bag:null,pet:null},purchased={};for(const slot of ['hat','glasses','bag','pet']){const id=draft[slot],info=itemInfo(id);legacy[slot]=id&&info?.builtUnlocked?id:null}const first=await timedFetch('/api/player/me/equipment',{method:'POST',headers:{'Content-Type':'application/json',...headers()},body:JSON.stringify({baseCharacter:draftBase,equipment:legacy})}),firstData=await first.json().catch(()=>({}));if(!first.ok||!firstData.ok)throw new Error(firstData.code||'legacy-equipment-save-failed');for(const slot of slots){const id=draft[slot],info=itemInfo(id);if(id&&owned.has(id)&&!info?.builtUnlocked)purchased[slot]=id;else if(shopData?.equipment?.[slot]&&(!id||!owned.has(id)))purchased[slot]=null}if(Object.keys(purchased).length){const second=await timedFetch('/api/shop/equipment',{method:'PUT',headers:{'Content-Type':'application/json',...headers()},body:JSON.stringify({equipment:purchased})}),secondData=await second.json().catch(()=>({}));if(!second.ok||!secondData.ok)throw new Error(secondData.code||'shop-equipment-save-failed')}await load();message.textContent='캐릭터 모습이 저장됐어요! ✨';window.dispatchEvent(new Event('studyvillage:ranking-refresh'));if(closeAfter){panel.hidden=true;button.focus?.()}return true}catch(err){console.warn('[StudyVillage customize save]',err);message.textContent='저장하지 못했어요. 잠시 후 다시 눌러 주세요.';return false}finally{save.disabled=false}}
  async function equipPurchasedNow(itemId,itemName){if(!await load())return;const info=itemInfo(itemId);if(!info||!ownedSet().has(itemId)){message.textContent='구매한 아이템을 찾지 못했어요.';return}draft[info.slot]=itemId;renderInventory();renderAvatar();const ok=await saveEquipment();message.textContent=ok?`✨ ${itemName||info.name}을(를) 바로 장착했어요!`:'구매는 완료됐지만 장착 저장은 다시 시도해 주세요.'}
  function active(){return !document.hidden&&navigator.onLine&&game?.classList.contains('active')}
  async function pollUnlocks(){if(polling||!active())return;polling=true;try{if(panel.hidden)await load()}finally{polling=false}}
  function startPolling(){if(pollTimer||!active())return;pollUnlocks();pollTimer=setInterval(pollUnlocks,POLL_MS)}
  function stopPolling(){if(pollTimer){clearInterval(pollTimer);pollTimer=null}}
  button.addEventListener('click',openPanel);close?.addEventListener('click',closePanel);save.addEventListener('click',()=>saveEquipment({closeAfter:true}));window.addEventListener('studyvillage:shop-purchase',()=>setTimeout(pollUnlocks,0));window.addEventListener('studyvillage:equip-purchased-item',e=>equipPurchasedNow(e.detail?.itemId,e.detail?.itemName));window.addEventListener('keydown',e=>{if(!panel.hidden&&e.key==='Escape'){e.stopImmediatePropagation();closePanel()}},true);if(game){const observer=new MutationObserver(()=>{if(game.classList.contains('active'))startPolling();else stopPolling()});observer.observe(game,{attributes:true,attributeFilter:['class']})}document.addEventListener('visibilitychange',()=>{if(document.hidden)stopPolling();else startPolling()});window.addEventListener('online',startPolling);window.addEventListener('offline',stopPolling);startPolling();
})();
