# Studyvillage Stability Notes

## v1.9.x — 현재 구현 상태 요약

- 점수 이상 감지는 `score-alert-policy.js`로 분리하고, 교사 수정·되돌리기·명시적 초기화 같은 정상 관리 경로는 기본 경고에서 억제한다.
- 새 `/api/admin/score-alerts` 읽기 경로는 기존 관리자 UI보다 먼저 설치되어 오탐 조정 정책을 적용하고, 기존 검토·수정·되돌리기 API와 응답 계약은 유지한다.
- 점수 알림 정책 자체검사와 관리자 UI 계약검사를 `npm run verify`에 포함한다.
- 문제 데이터는 자동 수정하지 않는다. 관리자 검토 큐에서 의심 문제를 확인하고, 교사가 직접 수정하며 수정 이력을 보존한다.
- 새 문제 세트 추가 기준은 `QUESTION_SET_TEMPLATE.md`를 사용한다. `activityId`, `subject`, `topic`, `questions`를 기본 구조로 하고 메타데이터 자체검사와 공통 점수/도전 정책 연결을 확인한다.
- 별은 별도 누적 재화와 변경 장부로 관리하고, 상점 구매 시 별 차감과 아이템 영구 소유를 서버 트랜잭션으로 처리한다.
- 학생 꾸미기 화면에서 별 상점, 구매, 구매 아이템 즉시 장착을 지원한다. 교사는 상점 ON/OFF와 가격을 설정할 수 있다.
- 활동별 도전 정책은 `1회 / N회 / 무제한`, XP는 `첫 완료에만 / 매 도전마다`를 지원한다.
- 수수께끼와 책마루 어휘 모두 서버에서 도전 제한을 검증한다.
- 교사는 학생별 남은 도전 횟수를 보고 특정 학생에게 추가 도전권을 지급할 수 있다.
- 추가 도전권은 실제 사용 시 자동 차감되며, 지급·직접 수정·사용 이력을 별도 장부로 남기고 관리자 화면에서 확인할 수 있다.
- 별 장부와 추가 도전권 이력 등 settings 기반 데이터가 백업에 보존되도록 검증 기반을 확장했다.
- 학생/관리자 점수 장부는 공통 `subject`/`topic` 메타데이터를 사용하고, 브라우저/서버 분류 불일치도 `npm run verify`에서 검사한다.
- 관리자 복원은 preflight와 서버 검증 미들웨어를 실제 `/api/admin/restore` 앞단에 연결했고, 잘못된 백업이면 실제 복원 핸들러가 실행되지 않는 자체검사를 포함한다.
- 활동 제출 재시도 시 점수/XP 중복 지급을 막고, 수수께끼의 기존 저장 경로까지 포함한 교차 경로 재시도 안전검사를 `npm run verify`에 포함한다.
- 관리자 polling, 학생 presence/live-events, 로그인/세션 복원 요청의 불필요한 중복·DB 읽기를 개발용 계약검사에서 확인한다.
- 교실 QR은 실제 IPv4 주소를 순위화해 추천 주소를 하나 표시하고, 가상/VPN 주소도 대체 주소로 보존한다.
- 교사 PC가 유선이고 학생 패드가 Wi-Fi여도 학교 유선망↔Wi-Fi망 사이 통신이 허용되면 정상 접속할 수 있음을 QR 화면과 빠른 실행 안내에 명시한다.
- 추천 QR은 화면에 크게 먼저 보이고, 나머지 주소는 “다른 접속 주소 보기”에 접어두며 이 UI 계약도 `npm run verify`에서 검사한다.

## v1.8.0 — 점수 감사 이력 백업/복원

- 백업 형식을 v9로 올리고 `score_ledger`, `score_alert_reviews`, `score_corrections`를 함께 저장한다.
- v8 이하 백업은 감사 이력 배열을 빈 값으로 추가하여 v9로 안전하게 변환한다.
- 복원 전 점수 장부/검토/수정 이력의 학생 참조, 값 범위, 중복 ID, 장부 참조 관계를 검증한다.
- 복원 과정에서 활동 기록 삽입·삭제로 임시 생성되는 장부 이력은 제거한 뒤 백업에 저장된 감사 이력을 그대로 복구한다.
- 기존 학생/관리자/활동 API는 변경하지 않고 백업·복원 데이터 범위만 확장한다.

## v1.7.0 — 교사 안전 수정 + 되돌리기

- `수정 필요`로 교사가 확인한 경고만 실제 점수/XP 수정 대상으로 허용한다.
- 수정 시 최종 값과 수정 이유를 교사가 직접 입력하고, 수정 전/후 값·사유·시각을 `score_corrections`에 별도로 남긴다.
- 실제 DB 값 변경은 기존 `score_ledger` 트리거도 함께 기록하므로 감사 이력이 이중으로 남는다.
- 수정 직전 현재 값을 다시 확인해 값이 달라졌으면 덮어쓰지 않고 중단한다.
- 교사 수정은 되돌릴 수 있다. 단, 수정 후 다른 변화가 발생했다면 안전을 위해 자동 되돌리기를 거부한다.
- 되돌리기 자체도 점수 장부에 새 변화로 기록된다.
- 학생 화면에는 내부 오류 판단/수정 도구를 노출하지 않는다.

## v1.6.0 — 교사용 이상 점수 알림

- `score_ledger`를 읽어 중복 반영 또는 비정상 변화로 의심되는 기록을 **자동 수정 없이 표시만** 한다.
- 교사용 API `/api/admin/score-alerts`와 관리자 화면의 `⚠ 확인 필요한 점수 기록` 패널을 추가한다.
- 현재 기본 진단 규칙: 한 번에 큰 XP 변화, 한 활동에서 1000점을 넘는 단일 증감, 전체 점수의 큰 단일 변화, 예상 밖 감소, 짧은 시간 안의 동일 증감 반복.
- 진단 규칙은 오류 확정 판정이 아니라 교사 확인을 위한 보수적 경고다.
- 교사는 경고에서 학생, 시각, 활동 ID, 변경 전/후 값, 증감량, 의심 사유를 확인할 수 있다.
- `이 학생 장부 보기`로 최근 점수/XP 변경 이력을 바로 확인한다.
- 교사는 경고를 `정상 기록으로 확인` 또는 `수정 필요 표시`로 분류할 수 있고, 수정 필요 시 메모를 남길 수 있다.
- 학생 화면에는 내부 오류 진단 경고를 노출하지 않는다.

## v1.5.0 — 점수/XP 변경 장부

- 학생 점수와 XP의 변경 원인을 나중에 추적할 수 있도록 `score_ledger`를 둔다.
- 기존 점수 계산 로직은 수정하지 않고 SQLite 트리거로 실제 DB 변경만 기록한다.
- 기록 항목: 학생, 범위(player/activity), 활동 ID, 필드, 변경 전 값, 변경 후 값, 증감량, 발생 경로, 시각.
- `players.total_score`, `players.xp`, `activity_records.total_score`의 삽입/변경/삭제를 기록한다.
- 교사용 조회 API `/api/admin/score-ledger`를 제공하고 학생 이름·활동 ID·조회 개수로 필터링할 수 있게 한다.
- release readiness에 점수 장부 트리거 연결 확인을 추가한다.
- 목적은 중복 지급, 비정상 점수, 초기화·삭제로 인한 큰 변동을 사후 추적하는 것이다.

## 데이터 수정 원칙

- 이상 데이터를 발견해도 프로그램이 문제 데이터나 점수 데이터를 임의로 자동 수정하지 않는다.
- 먼저 감사(audit)와 장부를 통해 이상 여부와 변경 경로를 보여준다.
- 실제 수정은 교사가 관리자 화면에서 내용을 확인한 뒤 명시적으로 실행한다.
- 수정 전/후 값, 수정 사유, 수정 시각을 반드시 남기고 되돌리기 가능한 구조를 우선한다.
- DB 직접 수정은 교사 기본 운영 흐름으로 노출하지 않는다.

## 학생 개인 점수/별/소비 이력 — 구현 상태

- 학생은 자신의 별 잔액과 별 변경/소비 이력을 확인할 수 있고, 상점 구매 차감도 장부에 남는다.
- 점수 장부 학생 조회 API가 있어 본인 점수/XP 변경 이력을 안전하게 읽을 수 있다.
- 학생 기록 화면에서 과목·주제별 누적 점수와 점수 변경 이력을 확인할 수 있다.
- 학생/관리자 서버 점수 이력은 공통 `activity-metadata.js`를 사용한다.
- 브라우저 `activity-taxonomy.js`와 서버 분류가 어긋나면 자동 계약검사에서 잡힌다.
- 별은 전 활동에서 함께 쓰는 누적 재화이므로 과목별로 나누지 않는다.

## 문제 데이터 교사 검토 — 구현 원칙

- 문제 데이터는 프로그램이 임의로 정답·보기·문구를 자동 수정하지 않는다.
- 문제 이상은 `확인 필요` 상태로 관리자 화면에 올리고, 교사가 원문/정답/보기/과목·주제 정보를 확인한 뒤 직접 수정한다.
- 교사가 수정하면 수정 전/후 내용과 수정 시각을 변경 이력으로 남긴다.
- 점수 이상 알림과 문제 데이터 검토 알림은 서로 구분해서 보여준다.
- 새 문제 추가는 `QUESTION_SET_TEMPLATE.md`를 기준으로 하며, 메타데이터 자체검사를 통과해야 한다.

## 활동별 도전 횟수 — 구현 상태

- 교사가 활동마다 `1회 / N회 / 무제한`을 설정할 수 있다.
- XP 지급 방식은 `첫 완료에만 / 매 도전마다`로 설정할 수 있다.
- 학생별 남은 횟수를 관리자에서 확인할 수 있다.
- 특정 학생에게 `+1회 허용`으로 추가 기회를 줄 수 있다.
- 무제한 활동에는 추가 도전권 버튼을 노출하지 않는다.
- 추가 도전권은 기본 허용 횟수를 모두 쓴 뒤에만 자동 사용한다.
- 지급·직접 수정·사용 기록은 별도 장부로 남기고 관리자에서 확인한다.
- 수수께끼 구형 스냅샷 저장 경로와 신규 활동 저장 경로 모두 정책을 서버에서 검증한다.

## v1.9 안정화 종료까지 남은 핵심

1. `npm run verify`와 GitHub Actions `Verify` 결과를 실제 배포 전 체크리스트에 포함한다.
2. 실제 교실 사용에서 `정상 기록으로 확인`된 점수 경고 비율을 보고 오탐 기준값을 필요할 때만 조정한다.
3. 실제 학교 유선 PC↔Wi-Fi 패드 환경에서 추천 QR로 `접속 → 로그인 → 활동 저장`까지 1대 실기 시험을 하고, 필요하면 네트워크 안내 문구만 미세 조정한다.

## 1.0 이후 백로그

- 장기 운영용 XP 레벨 곡선: 초기 레벨은 비교적 빠르고 고레벨은 점차 느려지는 RPG식 곡선을 별도 설계 후 적용한다.
- 학생 캐릭터 성장/레벨업 외형·칭호·해금 확장.
- 지도서 업로드 후 앱 내부에서 자동 문제 생성하는 기능은 현재 목표에서 제외한다. 지도서는 ChatGPT와 함께 문제를 만들고, 교사가 앱에서 수정하는 흐름을 기본으로 한다.
