# Studyvillage v1 최종 실기기 점검표

`stabilization-audit-20260818` 후보판을 `main`에 합치기 전 마지막 교실 점검입니다. 자동 검증을 통과해도 실제 수업 기기 확인 전에는 병합하지 않습니다.

## 1. 학생 — QR → 네이버/Whale 계열 (핵심, 약 8분)
1. QR로 접속 → 학생 로그인 → 새로고침 후 같은 학생으로 정상 복귀.
2. 빈 지도 여러 곳 터치/클릭 → 목적지 이동만 되고 방향키/WASD/Space/말하기 버튼이 없음.
3. 배움터 → 책마루 → 도전관을 각각 열고 닫기. 건물/HUD 터치가 지도 이동으로 먹히지 않음.
4. 도전관 → 수수께끼 → 쉬움/보통/어려움 중 하나 선택 → 시작 전 `오늘/전체 기간 N회 남음` 표시 확인.
5. 도전 완료 → 결과가 한 번만 저장되고 도전관으로 돌아온 뒤 남은 횟수가 정확히 1회 감소. 횟수 소진 시 재도전 차단.
6. 🧭 탐험 → 확대된 지도에서 이동 → 2~3명의 NPC 위치가 한곳에 고정되지 않음 → 먼 NPC는 접근 후 상호작용.
7. 숨은 발견물/단서가 처음부터 모두 보이지 않고, 이동하며 발견 → 마지막 지역 완료 → 결과 저장 → 마을 복귀.
8. 꾸미기 → 모자/안경/가방/펫 장착 → 꾸미기 미리보기와 마을 캐릭터 위치·크기가 크게 어긋나지 않음.
9. 내 기록에서 레벨/XP/별을 확인하고, 도전·탐험 완료 뒤 새로고침해도 값이 유지됨.

## 2. 학생 — iPad 실제 수업 브라우저 (핵심, 약 6분)
1. QR 접속/로그인 → 빈 지도 여러 곳 터치 → 페이지 스크롤 없이 캐릭터 이동.
2. 건물/HUD 직접 터치 → 캐릭터가 같이 움직이지 않고 해당 화면만 즉시 열림.
3. 배움터·책마루·도전관에서 버튼/입력칸이 화면 아래로 잘리거나 옛 방향키용 큰 빈 공간이 남지 않음.
4. 탐험 → 터치 이동·장애물 충돌·확대 지도 카메라 이동 → 먼 NPC 접근 → 숨은 발견 → 완료/복귀.
5. 세로/가로 회전 1회 → UI와 캐릭터가 화면 밖으로 크게 이탈하지 않음.
6. 브라우저를 잠깐 백그라운드로 보냈다가 복귀 → 이전 이동이 혼자 계속되지 않음.
7. 탐험/도전관 종료 후 마을 건물·HUD가 다시 정상 터치됨.

## 3. 교사 모드 — 관리자 홈 전체 연결 (핵심, 약 10분, 테스트 학생 1명)
1. 관리자 진입 → `학생 관리 / 활동·횟수 / 문제 관리 / 별·상점·아이템 / 탐험 관리 / 기록·백업·오류` 6개 메인 버튼을 차례로 열고 `관리자 홈`으로 복귀. 빈 화면이나 무한 로딩 없음.
2. 학생 관리 → XP·칭호·이름·비밀번호 중 테스트 가능한 값 1개 수정 → 새로고침 후 유지.
3. 별·상점·아이템 → 별 소량 지급 후 차감 → 학생 내 기록/잔액에 반영.
4. 활동·횟수 → 도전관 또는 탐험을 `매일 N회`로 설정 → 학생 화면 남은 횟수 확인 → 1회 완료 → 1회 감소 확인.
5. 같은 학생에게 `+1회 허용` → 학생 화면에 즉시 추가되고 사용 후 1회 감소.
6. 활동 열기/닫기 → 학생 접근 여부가 즉시 맞게 바뀜.
7. 문제 관리 → 문제 검토/편집 화면이 열리고 저장 가능한 항목 1개를 테스트(실제 문제를 바꾸기 싫으면 저장 직전 취소 가능).
8. 기록·백업·오류 → 최근 활동/감사 기록에 위 교사 작업이 남는지 확인.
9. 백업 JSON 다운로드 → 눈에 보이는 테스트 변경 1개 → 복원 → XP/별/장비/활동 상태가 백업 시점으로 돌아오는지 확인.
10. 상태 메모 등 기존에 수정이 안 되던 항목이 있다면 다시 한 번 저장 후 새로고침해 유지 여부 확인.

## 4. 보조 확인 — Chrome
Chrome은 이번 교실 V1 후보판의 **병합 차단 조건이 아닙니다.** 시간이 있을 때 로그인/지도 이동 정도만 확인하고, 실제 운영 판단은 QR→네이버/Whale 계열과 iPad 결과를 우선합니다.

## 실패 시 기록할 것
- 기기/브라우저
- 학생 또는 교사 화면
- 누른 순서 2~5단계
- 기대한 결과 / 실제 결과
- 가능하면 화면 캡처 1장

## Release gate
**QR→네이버/Whale 계열, iPad 터치, 교사 관리자 홈/쓰기/횟수/백업·복원**이 모두 막힘 없이 통과한 뒤에만 `main`으로 승격합니다. 하나라도 실패하면 `stabilization-audit-20260818`에서만 수정 → 전체 CI → 실패한 실기기 경로 재시험 순서로 진행합니다.
