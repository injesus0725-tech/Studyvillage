const fs=require('fs');
const assert=require('assert');
const interiors=fs.readFileSync('building-interiors.js','utf8');
assert.ok(interiors.includes("retiredQuizHall=document.querySelector('#quiz-hall')"),'retired quiz hall guard missing');
assert.ok(interiors.includes('function blockRetiredQuizInteraction'),'retired quiz interaction blocker missing');
assert.ok(!interiors.includes("action:'quiz'"),'retired riddle hall action returned');
assert.ok(!interiors.includes('수수께끼 도전 시작'),'retired riddle action button returned');
assert.ok(interiors.includes("action:'math'"),'math playground entry must remain');
assert.ok(interiors.includes("action:'library'"),'Bookmaru entry must remain');
console.log('retired riddle entry contract self-test passed');
