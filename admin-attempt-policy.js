/* Daily classroom attempt controls. Core activities reset at Korea midnight. */
(()=>{
  const app=document.querySelector('#admin-app');if(!app)return;
  const headers=json=>{const token=sessionStorage.getItem('studyvillage-admin-token')||'';return token?{Authorization:`Bearer ${token}`,...(json?{'Content-Type':'application/json'}:{})}:{...(json?{'Content-Type':'application/json'}:{})}};
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const REQUEST_TIMEOUT_MS=5000;
  const names={'riddle-demo':'도전관 · 수수께끼','library-vocabulary':'책마루 · 낱말 뜻 맞추기','math-arithmetic':'배움터 · 랜덤 계산 연습','exploration-forest-riddle':'생각의 숲 · 수수께끼','exploration-mountain-riddle':'도전의 산 · 수수께끼'};
  const CORE_IDS=['riddle-demo','library-vocabulary','math-arithmetic','exploration-forest-riddle','exploration-mountain-riddle'];
  const defaults={'riddle-demo':1,'library-vocabulary':1,'math-arithmetic':3,'exploration-forest-riddle':3,'exploration-mountain-riddle':3};
  async function timedFetch(url,options={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),REQUEST_TIMEOUT_MS);try{return await fetch(url,{...options,signal:controller.signal})}finally{clearTimeout(timer)}}
  const panel=document.createElement('section');panel.className='panel';panel.id='attempt-policy-panel';panel.innerHTML='<div class="panel-head"><div><h2>🎯 활동 도전 횟수</h2><p>모든 활동 횟수는 매일 밤 12시(한국 시간)에 자동으로 다시 시작합니다.</p></div><span id="attempt-policy-status" class="status">불러오는 중…</span></div><div id="attempt-policy-list" class="teacher-activity-grid"></div><div style="margin-top:12px"><button id="attempt-policy-save">하루 횟수 저장</button></div><div style="margin-top:18px"><div class="panel-head"><div><h3>👩‍🏫 학생별 오늘 남은 횟수</h3><p>필요한 학생에게만 오늘 추가 도전 1회를 허용할 수 있습니다.</p></div></div><div id="attempt-overview-list" class="teacher-activity-grid"><p class="empty">도전 횟수를 확인하는 중입니다.</p></div></div><div style="margin-top:18px"><div class="panel-head"><div><h3>📒 추가 도전권 이력</h3><p>최근 지급·수정·사용 기록을 확인합니다.</p></div><button type="button" id="attempt-history-refresh">새로고침</button></div><div id="attempt-history-list"><p class="empty">이력을 불러오는 중입니다.</p></div></div>';
  const anchor=document.querySelector('#class-activity-overview')?.closest('.panel');anchor?.after(panel);if(!anchor)app.appendChild(panel);
  const list=panel.querySelector('#attempt-policy-list'),overview=panel.querySelector('#attempt-overview-list'),historyList=panel.querySelector('#attempt-history-list'),status=panel.querySelector('#attempt-policy-status'),save=panel.querySelector('#attempt-policy-save'),historyRefresh=panel.querySelector('#attempt-history-refresh');let policies={},loading=false,historyLoading=false;
  function render(){list.innerHTML='';for(const id of CORE_IDS){const p=policies[id]||{},limit=Math.max(1,Number(p.limit)||defaults[id]),card=document.createElement('article');card.className='attention-card';card.dataset.activityId=id;card.innerHTML=`<div class="attention-card-head"><strong>${esc(names[id])}</strong><span>매일 00:00 갱신</span></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px"><label>하루 도전 횟수<input data-policy-limit type="number" min="1" max="1000" value="${limit}"></label><label>XP 지급<select data-policy-xp><option value="first-completion" ${p.xpMode==='every-attempt'?'':'selected'}>첫 완료에만</option><option value="every-attempt" ${p.xpMode==='every-attempt'?'selected':''}>매 도전마다</option></select></label></div>`;list.appendChild(card)}}
  function renderOverview(data){overview.innerHTML='';const activities=(data?.activities||[]).filter(a=>CORE_IDS.includes(a.activityId));if(!activities.length){overview.innerHTML='<p class="empty">학생별 오늘 남은 횟수를 불러오지 못했습니다.</p>';return}for(const activity of activities){const section=document.createElement('article');section.className='attention-card';section.innerHTML=`<div class="attention-card-head"><strong>${esc(names[activity.activityId]||activity.activityId)}</strong><span>오늘 ${activity.policy?.limit||defaults[activity.activityId]}회</span></div>`;const rows=document.createElement('div');rows.style.cssText='display:grid;gap:6px;margin-top:10px';for(const s of activity.students||[]){const row=document.createElement('div');row.style.cssText='display:grid;grid-template-columns:minmax(80px,1fr) auto auto;gap:8px;align-items:center;padding:8px 0;border-top:1px solid #e7ece8';const extra=Number(s.extraAttempts)||0,remaining=Math.max(0,Number(s.remaining)||0),base=Math.max(0,remaining-extra),label=extra?`오늘 남음 ${base+extra}회 · 추가 ${extra}회`:`오늘 남음 ${base}회`;row.innerHTML=`<strong>${esc(s.name)}</strong><span>${label}</span><button type="button" data-grant-name="${esc(s.name)}" data-grant-activity="${esc(activity.activityId)}">+1회 허용</button>`;rows.appendChild(row)}section.appendChild(rows);overview.appendChild(section)}}
  function renderHistory(entries=[]){historyList.innerHTML='';if(!entries.length){historyList.innerHTML='<p class="empty">아직 추가 도전권 이력이 없습니다.</p>';return}const typeText={grant:'지급',set:'수정',consume:'사용'},rows=document.createElement('div');rows.style.cssText='display:grid;gap:6px';for(const e of entries.slice(0,100)){const row=document.createElement('div');row.style.cssText='display:grid;grid-template-columns:minmax(90px,.8fr) minmax(120px,1fr) auto minmax(120px,1.4fr);gap:8px;align-items:center;padding:8px 0;border-top:1px solid #e7ece8';const time=e.createdAt?new Date(e.createdAt).toLocaleString('ko-KR'):'-',activity=names[e.activityId]||e.activityId,delta=Number(e.amount)||0;row.innerHTML=`<strong>${esc(e.name)}</strong><span>${esc(activity)}</span><span>${esc(typeText[e.type]||e.type)} ${delta>0?`+${delta}`:delta}회</span><small>${esc(e.detail||'')}<br>${esc(time)}</small>`;rows.appendChild(row)}historyList.appendChild(rows)}
  async function loadHistory(){if(historyLoading)return;historyLoading=true;try{const r=await timedFetch('/api/admin/activity-attempt-extra-history?limit=100',{headers:headers(),cache:'no-store'}),d=await r.json();if(!r.ok||!d.ok)throw new Error();renderHistory(d.entries||[])}catch{historyList.innerHTML='<p class="empty">추가 도전권 이력을 불러오지 못했습니다.</p>'}finally{historyLoading=false}}
  async function loadOverview(){try{const r=await timedFetch('/api/admin/activity-attempt-overview',{headers:headers(),cache:'no-store'}),d=await r.json();if(!r.ok||!d.ok)throw new Error();renderOverview(d)}catch{overview.innerHTML='<p class="empty">학생별 오늘 남은 횟수를 불러오지 못했습니다.</p>'}}
  async function load(){if(loading)return;loading=true;try{const r=await timedFetch('/api/admin/activity-attempt-policies',{headers:headers(),cache:'no-store'}),d=await r.json();if(!r.ok||!d.ok)throw new Error();policies=d.policies||{};render();status.textContent='매일 00:00 갱신';await Promise.all([loadOverview(),loadHistory()])}catch{status.textContent='설정 불러오기 실패'}finally{loading=false}}
  async function savePolicies(){const next={};for(const card of list.querySelectorAll('[data-activity-id]')){const id=card.dataset.activityId,limit=Math.max(1,Number(card.querySelector('[data-policy-limit]').value)||defaults[id]),xpMode=card.querySelector('[data-policy-xp]').value;next[id]={mode:'limited',limit,xpMode}}save.disabled=true;status.textContent='저장 중…';try{const r=await timedFetch('/api/admin/activity-attempt-policies',{method:'PUT',headers:headers(true),body:JSON.stringify({policies:next})}),d=await r.json();if(!r.ok||!d.ok)throw new Error();policies=d.policies||next;render();status.textContent='저장 완료 · 매일 00:00 갱신';await loadOverview()}catch{status.textContent='저장 실패';alert('하루 도전 횟수를 저장하지 못했습니다.')}finally{save.disabled=false}}
  overview.addEventListener('click',async e=>{const b=e.target.closest('button[data-grant-name]');if(!b)return;const name=b.dataset.grantName,activityId=b.dataset.grantActivity;if(!confirm(`${name} 학생에게 ${names[activityId]} 오늘 추가 도전 1회를 허용할까요?`))return;b.disabled=true;try{const r=await timedFetch(`/api/admin/activity-attempt-extra/${encodeURIComponent(name)}/${encodeURIComponent(activityId)}/grant`,{method:'POST',headers:headers(true),body:JSON.stringify({amount:1})}),d=await r.json();if(!r.ok||!d.ok)throw new Error();await Promise.all([loadOverview(),loadHistory()])}catch{alert(`${name} 학생에게 추가 도전을 허용하지 못했습니다.`)}finally{b.disabled=false}});
  historyRefresh?.addEventListener('click',loadHistory);save.addEventListener('click',savePolicies);document.querySelector('#refresh-button')?.addEventListener('click',()=>setTimeout(load,150));window.addEventListener('load',()=>setTimeout(load,500));
})();
