/* Stabilization: cave/forest map decor can be approached and inspected instead of acting as dead scenery. */
(()=>{
  const stage=document.querySelector('#study-expedition-stage');if(!stage)return;
  let pending=null,raf=0;
  const names={'💎':'반짝 수정','🪨':'동굴 바위','🌿':'작은 나뭇잎','🌲':'큰 나무','🌳':'오래된 나무','🍄':'버섯','🌼':'들꽃','🪵':'통나무','💧':'샘물','🌷':'꽃','🕯️':'촛불','🦇':'박쥐','🧱':'돌벽','🔥':'횃불','🗝️':'열쇠','🛡️':'방패','⚔️':'검','🚪':'문','🏚️':'유적','🗿':'석상','🏛️':'기둥','🌙':'달빛','🏰':'성','🚩':'깃발','👑':'왕관','🏯':'성문'};
  const style=document.createElement('style');style.textContent='.sv-stage-decor{cursor:pointer;pointer-events:auto;user-select:none;transition:transform .16s ease,filter .16s ease}.sv-stage-decor:hover,.sv-stage-decor:focus{transform:scale(1.12);outline:none}.sv-stage-decor.near-find{filter:drop-shadow(0 0 8px #ffe36a) drop-shadow(0 5px 4px #22352833)}.sv-stage-decor.inspected{filter:drop-shadow(0 0 6px #8be58f) drop-shadow(0 5px 4px #22352833)}.sv-decor-toast{position:absolute;left:50%;bottom:7%;z-index:20;transform:translateX(-50%);padding:9px 13px;border:3px solid #fff;border-radius:999px;background:#31583eea;color:#fff;font-size:12px;font-weight:1000;box-shadow:0 7px 20px #172b1e55;pointer-events:none}';document.head.appendChild(style);
  function map(){return stage.querySelector('[data-stage-map]')}function hero(){return map()?.querySelector('.sv-stage-player')}
  function near(node){const h=hero(),host=map();if(!h||!node||!host)return false;const a=h.getBoundingClientRect(),b=node.getBoundingClientRect();return Math.hypot(a.left+a.width/2-(b.left+b.width/2),a.top+a.height/2-(b.top+b.height/2))<100}
  function label(node){const icon=(node.textContent||'✨').trim();return names[icon]||'탐험 발견물'}
  function toast(text){const host=map();if(!host)return;host.querySelector('.sv-decor-toast')?.remove();const el=document.createElement('div');el.className='sv-decor-toast';el.textContent=text;host.appendChild(el);setTimeout(()=>el.remove(),1800)}
  function inspect(node){pending=null;node.classList.remove('near-find');node.classList.add('inspected');node.dataset.inspected='1';toast(`🔎 ${label(node)}을(를) 살펴봤어요!`)}
  function approach(node){const host=map(),move=window.StudyVillageExpeditionMovement;if(!host||!move?.setTarget)return;const r=host.getBoundingClientRect(),t=node.getBoundingClientRect();pending=node;move.setTarget(t.left-r.left+t.width/2-46,t.top-r.top+t.height/2);watch()}
  function watch(){cancelAnimationFrame(raf);const tick=()=>{if(!pending||stage.hidden||!map()?.contains(pending)){pending=null;return}const all=map().querySelectorAll('.sv-stage-decor');all.forEach(n=>n.classList.toggle('near-find',near(n)));if(near(pending)){inspect(pending);return}raf=requestAnimationFrame(tick)};raf=requestAnimationFrame(tick)}
  function prepare(){const host=map();if(!host)return;for(const node of host.querySelectorAll('.sv-stage-decor')){if(node.dataset.inspectReady)return;node.dataset.inspectReady='1';node.setAttribute('role','button');node.tabIndex=0;const act=event=>{event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();if(node.dataset.inspected==='1')return toast(`✅ ${label(node)}은(는) 이미 살펴봤어요.`);if(near(node))inspect(node);else{toast(`🚶 ${label(node)} 가까이 가는 중…`);approach(node)}};node.addEventListener('click',act,true);node.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();act(event)}},true)}}
  new MutationObserver(()=>prepare()).observe(stage,{childList:true,subtree:true});stage.addEventListener('pointermove',()=>{map()?.querySelectorAll('.sv-stage-decor').forEach(n=>n.classList.toggle('near-find',near(n)))},{passive:true});window.addEventListener('studyvillage:session-cleared',()=>{pending=null;cancelAnimationFrame(raf)});setTimeout(prepare,300);
})();
