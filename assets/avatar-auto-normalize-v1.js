/*
 * RETIRED COMPATIBILITY STUB
 *
 * Production avatar assets are pre-aligned on the shared 256x256 canvas.
 * Runtime image analysis, canvas-based rewriting, observer-driven normalization,
 * and whole-image bounds alignment are intentionally disabled.
 *
 * This empty file temporarily remains only because legacy verification contracts may
 * still read this path. It performs no work when loaded.
 */
(function () {
  'use strict';
  // Intentionally empty.
})();
