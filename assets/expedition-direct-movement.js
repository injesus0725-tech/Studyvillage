/* Stabilization: expedition rooms use tap/click destination movement with safe walkable bounds, decor collision, and proximity interaction. */
(()=>{
  const stage=document.querySelector('#study-expedition-stage');if(!stage)return;
  const clamp=(v,min,max)=>Math.max(min,Math.min(max,v));let target=null,raf=0,last=0;
  function map(){return stage.querySelector('[data-stage-map]')}
  function player(){return map()?.querySelector('.sv-stage-player')}
  function npc(){return map()?.querySelector('.sv-stage-npc')}
  function center(el,host){const a=el.getBoundingClientRect(),b=host.getBoundingClientRect();return{x:a.left-b.left+a.width/2,y:a.top-b.top+a.height/2}}
  function bounds(host,hero){const w=hero.offsetWidth||70,h=hero.offsetHeight||70;return{left:w/2+8,right:Math.max(w/2+8,host.clientWidth-w/2-8),top:h/2+38,bottom:Math.max(h/2+38,host.clientHeight-h/2-12)}}
  function safePoint(host,hero,x,y){const b=bounds(host,hero);return{x:clamp(x,b.left,b.right),y:clamp(y,b.top,b.bottom)}}
  function obstacleRects(host){const root=host.getBoundingClientRect();return[...host.querySelectorAll('.sv-stage-decor')].map(el=>{const r=el.getBoundingClientRect(),pad=10;return{left:r.left-root.left-pad,right:r.right-root.left+pad,top:r.top-root.top-pad,bottom:r.bottom-root.top+pad}})}
  function canStand(host,hero,x,y){const p=safePoint(host,hero,x,y),radius=Math.max(18,Math.min(hero.offsetWidth||70,hero.offsetHeight||70)*.28);return !obstacleRects(host).some(o=>p.x+radius>o.left&&p.x-radius<o.right&&p.y+radius>o.top&&p.y-radius<o.bottom)}
  function place(el,host,x,y){const p=safePoint(host,el,x,y),w=el.offsetWidth||70,h=el.offsetHeight||70;el.style.left=`${p.x-w/2}px`;el.style.top=`${p.y-h/2}px`;el.style.right='auto';el.style.bottom='auto'}
  function direction(dx,dy){if(Math.abs(dx)>Math.abs(dy))return dx<0?'left':'right';return dy<0?'up':'down'}
  function setMotion(hero,moving,dir){hero.dataset.motion=moving?'walk':'idle';hero.dataset.direction=dir||hero.dataset.direction||'down';hero.classList.toggle('is-moving',moving);window.StudyVillageAvatar?.setMotion?.(hero,{moving,direction:hero.dataset.direction})}
  function nextStep(host,hero,p,dx,dy,step){const len=Math.hypot(dx,dy)||1,ux=dx/len,uy=dy/len,direct={x:p.x+ux*step,y:p.y+uy*step};if(canStand(host,hero,direct.x,direct.y))return direct;for(const side of [1,-1]){const candidate={x:p.x-uy*step*1.8*side,y:p.y+ux*step*1.8*side};if(canStand(host,hero,candidate.x,candidate.y))return candidate}return null}
  function stop(){target=null;last=0;if(raf){cancelAnimationFrame(raf);raf=0}const hero=player();if(hero)setMotion(hero,false)}
  function animate(now=performance.now()){raf=0;const host=map(),hero=player();if(!host||!hero||!target||stage.hidden)return stop();const p=center(hero,host),dx=target.x-p.x,dy=target.y-p.y,dist=Math.hypot(dx,dy);if(dist<4){if(canStand(host,hero,target.x,target.y))place(hero,host,target.x,target.y);return stop()}const elapsed=Math.min(32,Math.max(8,now-(last||now-16))),step=Math.min(260*elapsed/1000,dist);last=now;const next=nextStep(host,hero,p,dx,dy,step);if(!next)return stop();place(hero,host,next.x,next.y);setMotion(hero,true,direction(next.x-p.x,next.y-p.y));raf=requestAnimationFrame(animate)}
  function setTarget(x,y){const host=map(),hero=player();if(!host||!hero)return;let point=safePoint(host,hero,x,y);if(!canStand(host,hero,point.x,point.y)){const start=center(hero,host),dx=point.x-start.x,dy=point.y-start.y,len=Math.hypot(dx,dy)||1;for(let d=20;d<=100;d+=20){const candidate=safePoint(host,hero,point.x-dx/len*d,point.y-dy/len*d);if(canStand(host,hero,candidate.x,candidate.y)){point=candidate;break}}}target=point;last=performance.now();if(!raf)raf=requestAnimationFrame(animate)}
  function nearNpc(guide=npc()){const host=map(),hero=player();if(!host||!hero||!guide)return false;const a=center(hero,host),b=center(guide,host);return Math.hypot(a.x-b.x,a.y-b.y)<=Math.max(110,guide.offsetWidth*.92)}
  function approachNpc(guide){const host=map(),hero=player();if(!host||!hero||!guide)return;const g=center(guide,host),h=center(hero,host),dx=h.x-g.x,dy=h.y-g.y,len=Math.hypot(dx,dy)||1,distance=Math.max(88,guide.offsetWidth*.68);setTarget(g.x+dx/len*distance,g.y+dy/len*distance)}
  stage.addEventListener('pointerup',event=>{const host=map();if(!host||stage.hidden||(event.pointerType==='mouse'&&event.button!==0))return;if(event.target.closest('.sv-stage-question,.sv-stage-result,.sv-stage-head,.sv-stage-find'))return;const guide=event.target.closest('.sv-stage-npc');if(guide){if(nearNpc(guide)){guide.click();return}approachNpc(guide);return}const r=host.getBoundingClientRect();setTarget(event.clientX-r.left,event.clientY-r.top)},true);
  stage.addEventListener('click',event=>{const guide=event.target.closest('.sv-stage-npc');if(guide&&!nearNpc(guide)){event.preventDefault();event.stopImmediatePropagation()}},true);
  function reset(){stop();const host=map(),hero=player();if(host&&hero&&!stage.hidden){hero.style.position='absolute';const ratioX=Number(host.dataset.startX),ratioY=Number(host.dataset.startY),x=Number.isFinite(ratioX)?host.clientWidth*ratioX:Math.max(55,host.clientWidth*.13),y=Number.isFinite(ratioY)?host.clientHeight*ratioY:Math.max(90,host.clientHeight*.72);let p=safePoint(host,hero,x,y);if(!canStand(host,hero,p.x,p.y)){const fallbacks=[[.16,.65],[.12,.78],[.28,.78],[.18,.34],[.72,.78]];for(const [fx,fy]of fallbacks){const candidate=safePoint(host,hero,host.clientWidth*fx,host.clientHeight*fy);if(canStand(host,hero,candidate.x,candidate.y)){p=candidate;break}}}place(hero,host,p.x,p.y);setMotion(hero,false,p.x>host.clientWidth*.55?'left':'right')}}
  new MutationObserver(mutations=>{if(mutations.some(m=>m.type==='attributes'||[...m.addedNodes].some(n=>n.nodeType===1&&(n.matches?.('.sv-stage-player')||n.querySelector?.('.sv-stage-player')))))requestAnimationFrame(reset)}).observe(stage,{attributes:true,attributeFilter:['hidden'],childList:true,subtree:true});
  window.addEventListener('resize',()=>{const host=map(),hero=player();if(host&&hero&&!stage.hidden){const p=center(hero,host);place(hero,host,p.x,p.y)}});window.addEventListener('studyvillage:session-cleared',stop);
  window.StudyVillageExpeditionMovement={setTarget,nearNpc,reset,stop,canStand};
  if(!document.querySelector('script[data-expedition-lifecycle-stabilizer]')){const script=document.createElement('script');script.src='assets/expedition-lifecycle-stabilizer.js?v=20260818a';script.dataset.expeditionLifecycleStabilizer='1';document.head.appendChild(script)}
  if(!document.querySelector('script[data-expedition-npc-variety]')){const script=document.createElement('script');script.src='assets/expedition-npc-variety.js?v=20260819a';script.dataset.expeditionNpcVariety='1';document.body.appendChild(script)}
})();
