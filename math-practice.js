/* Server-issued arithmetic input practice. */
(()=>{const game=document.querySelector('#game-screen');if(!game)return;const ID='math-arithmetic',auth=()=>window.StudyVillageAuth?.authHeaders?.()||{},player=()=>document.querySelector('#profile-name')?.textContent?.trim()||'',checkpoint=()=>window.StudyVillageCheckpoint;let sessionId='',problems=[],answers=[],index=0,busy=false;const panel=document.createElement('section');panel.className='math-practice-panel';panel.hidden=true;panel.innerHTML='<div class="math-practice-card"><button class="quiz-close" type="button">✕</button><span class="library-badge">➕ 배움터 수학</span><h2>랜덤 계산 연습</h2><div class="library-status"><span data-progress></span><strong>직접 입력형</strong></div><p class="math-prompt"></p><input class="library-answer-input" type="number" inputmode="decimal" step="any" autocomplete="off" placeholder="정답 입력"><p class="library-feedback"></p><button class="interior-primary" type="button" data-submit>정답 제출</button></div>';game.appendChild(panel);const close=panel.querySelector('.quiz-close'),progress=panel.querySelector('[data-progress]'),prompt=panel.querySelector('.math-prompt'),input=panel.querySelector('input'),feedback=panel.querySelector('.library-feedback'),submit=panel.querySelector('[data-submit]');async function fetchJson(url,options={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),5000);try{const response=await fetch(url,{...options,headers:{...auth(),...(options.headers||{})},signal:controller.signal}),data=await response.json().catch(()=>({}));if(!response.ok||!data.ok)throw Object.assign(new Error(data.code||'request-failed'),{code:data.code});return data}finally{clearTimeout(timer)}}function saveCheckpoint(){if(sessionId&&player()&&checkpoint())checkpoint().save(player(),ID,{sessionId,problems,index,answers})}function clearCheckpoint(){if(player()&&checkpoint())checkpoint().clear(player(),ID)}function render(){progress.textContent=`${index+1} / ${problems.length}`;prompt.textContent=problems[index].prompt;input.value=answers[index]??'';input.disabled=false;submit.disabled=false;submit.textContent=index===problems.length-1?'채점하고 완료':'정답 저장하고 다음';feedback.textContent='';setTimeout(()=>input.focus(),0);saveCheckpoint()}async function start(){if(busy)return;busy=true;try{const allowance=await fetchJson('/api/player/me/activity-attempt-status/'+ID);if(!allowance.allowed){alert('계산 연습 참여 횟수를 모두 사용했어요.');return}const saved=player()&&checkpoint()?checkpoint().load(player(),ID)?.progress:null;if(saved?.sessionId&&Array.isArray(saved.problems)&&saved.problems.length===5&&confirm(`이전에 풀던 계산 연습을 ${Number(saved.index)+1}번부터 이어갈까요?`)){sessionId=saved.sessionId;problems=saved.problems;answers=Array.isArray(saved.answers)?saved.answers:[];index=Math.max(0,Math.min(4,Number(saved.index)||0))}else{if(saved)clearCheckpoint();const data=await fetchJson('/api/player/me/math-practice/start',{method:'POST'});sessionId=data.sessionId;problems=data.problems;answers=[];index=0}panel.hidden=false;render()}catch{alert('수학 문제를 준비하지 못했어요. 교실 서버 연결을 확인해 주세요.')}finally{busy=false}}async function next(){if(busy)return;const value=Number(input.value);if(!Number.isFinite(value)||Math.abs(value)>1000000)return alert('숫자를 입력해 주세요.');answers[index]=value;if(index<problems.length-1){index++;render();return}busy=true;input.disabled=true;submit.disabled=true;feedback.textContent='서버가 정답을 채점하고 있어요.';saveCheckpoint();try{const graded=await fetchJson(`/api/player/me/math-practice/${encodeURIComponent(sessionId)}/answers`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answers})});const saved=await fetchJson('/api/player/me/activity',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({activityId:ID,score:graded.score,submissionId:graded.submissionId})});clearCheckpoint();feedback.className='library-feedback success';feedback.textContent=`${graded.total}문제 중 ${graded.correct}문제 정답 · ${graded.score}점 · +${Number(saved.gainedXp)||0} XP`;prompt.textContent='계산 연습 완료! 🎉';submit.disabled=false;submit.textContent='배움터로 돌아가기';submit.onclick=()=>{panel.hidden=true;submit.onclick=next};window.dispatchEvent(new Event('studyvillage:activity-record-refresh'))}catch(error){feedback.className='library-feedback error';feedback.textContent=error.code==='math-session-not-found'?'이어하기 시간이 지나 새 문제로 다시 시작해야 해요.':'결과를 저장하지 못했어요. 다시 눌러 주세요.';submit.disabled=false;submit.textContent=error.code==='math-session-not-found'?'새 연습 시작':'채점 다시 시도';if(error.code==='math-session-not-found')submit.onclick=()=>{clearCheckpoint();panel.hidden=true;submit.onclick=next;start()}}finally{busy=false}}submit.onclick=next;input.addEventListener('keydown',event=>{if(event.key==='Enter'){event.preventDefault();submit.click()}});close.onclick=()=>{if(!busy)panel.hidden=true};window.addEventListener('studyvillage:open-math-practice',start)})();
