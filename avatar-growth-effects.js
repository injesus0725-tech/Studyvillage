/* Lightweight level-based avatar expression. */
(()=>{const player=document.querySelector('#player'),levelLabel=document.querySelector('#profile-level'),game=document.querySelector('#game-screen');if(!player||!levelLabel||!game)return;const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;let level=1,lastTrail=0,raf=0;function sync(){const match=levelLabel.textContent.match(/\d+/);level=Math.max(1,Number(match?.[0])||1);player.dataset.growthTier=level>=5?'5':level>=3?'3':'1'}function trail(now){raf=0;if(!reduced&&!document.hidden&&game.classList.contains('active')&&level>=3&&player.dataset.motion==='walk'&&now-lastTrail>=220){lastTrail=now;const mark=document.createElement('span');mark.className=`avatar-growth-trail tier-${level>=5?'5':'3'}`;mark.textContent=level>=5?'✧':'✦';mark.style.left=player.style.left;mark.style.top=player.style.top;player.parentElement?.appendChild(mark);setTimeout(()=>mark.remove(),700)}raf=requestAnimationFrame(trail)}new MutationObserver(sync).observe(levelLabel,{childList:true,characterData:true,subtree:true});sync();raf=requestAnimationFrame(trail);window.addEventListener('pagehide',()=>cancelAnimationFrame(raf),{once:true})})();
