# StudyVillage 캐릭터·아이템 규격

최종 갱신: 2026-08-30

> 이 문서의 과거 다중 슬롯 규격은 폐기되었다. 제작 방향과 인계 기준은 `AVATAR_PRODUCTION_DIRECTION.md`가 최우선이다.

## 공통 기준

- 모든 생산 그래픽의 마스터 좌표계는 **256×256**이다.
- 캐릭터 중심점은 발 중앙이며 마을·꾸미기 미리보기·상점·랭킹에서 같은 상대 좌표를 사용한다.
- 아이템별 `x/y/scale` 예외 보정은 금지한다. 하나가 안 맞으면 해당 원본 또는 공통 정규화 알고리즘을 고친다.
- PNG의 투명 여백 차이는 런타임 공통 정규화기가 실제 불투명 픽셀 범위를 측정하여 제거한다.
- 기본 캐릭터 변형은 기존 기본 남자·여자와 같은 몸체 크기·목 위치·어깨 위치·발 중앙·캔버스를 사용한다.
- 기본 캐릭터 변형에서 달라지는 것은 머리 모양과 얼굴 표정뿐이다.
- 장착 순서는 `character/outfit/effect/pet`이다.
- 별도 `hair` 슬롯은 사용하지 않는다.
- 구형 슬롯과 테스트 계정 구매 데이터는 보존하지 않는다.

## 마스터 앵커

| 슬롯 | 공통 좌표/크기 | 의미 |
|---|---|---|
| character | 256×256 전신 마스터 | 승인된 기본 몸체를 그대로 사용 |
| outfit | centerX 128 / bottomY 246 / maxHeight 180 / maxWidth 248 | 모든 의상을 같은 몸 중심·발바닥에 자동 정렬 |
| effect | 캐릭터 중심 기준 | 몸 주변 효과용 공통 좌표 |
| pet | centerX 180 / bottomY 246 / maxHeight 112 / maxWidth 96 | 모든 펫을 캐릭터 오른쪽 다리 가까이에 자동 정렬 |

## 한벌 의상 규격

- 얼굴과 머리를 덮지 않는다. 의상 시각 요소는 원칙적으로 목 아래에만 존재한다.
- 원본 PNG의 투명 여백과 무관하게 실제 불투명 픽셀을 읽어 `centerX 128`, `bottomY 246`, `maxHeight 180`, `maxWidth 248` 공통 규격으로 정규화한다.
- 검·지팡이처럼 한쪽으로 튀어나온 장식 때문에 몸통이 밀리지 않도록 단순 바운딩 박스 중앙이 아니라 불투명 픽셀 분포의 중앙값을 사용한다.
- 목과 어깨가 기본 몸체에 자연스럽게 붙어 보여야 하며 머리와 몸 사이가 벌어지면 안 된다.
- 기본 남자·여자 및 모든 기본 캐릭터 변형에서 동일한 기준점으로 호환되어야 한다.
- `수호 기사만 -3px`, `마법사만 1.08배` 같은 개별 CSS 보정은 만들지 않는다.

## 펫 규격

- 모든 펫은 `centerX 180`, `bottomY 246`, 최대 높이 112px의 단일 앵커를 사용한다.
- 캐릭터 발 옆에 붙어 보이고 작은 화면에서 무릎~허벅지 정도 높이로 읽혀야 한다.
- 펫마다 별도 `left/right/translate/scale` 값은 만들지 않는다.
- 종별 실루엣을 분명히 다르게 만든다.

## 기본 캐릭터 생산 게이트

- 현재 승인되지 않은 과거 `character-boy-02~05`, `character-girl-02~05` 이미지는 판매용으로 사용하지 않는다.
- 새 기본 캐릭터는 승인된 기본 남자/여자 몸체를 복제한 뒤 **머리·표정만 교체**해서 만든다.
- 새 기본 캐릭터 그림이 실제 제작·검수되기 전에는 상점 상품으로 등록하지 않는다.

## 완료 검사

- 기본 남자·여자에서 의상을 바꿔도 몸 중심과 발 위치가 변하지 않아야 한다.
- 의상 6종이 모두 같은 공통 규칙만으로 정렬되어야 하며 개별 보정값이 0개여야 한다.
- 모든 펫이 같은 공통 앵커만으로 다리 가까이에 배치되어야 한다.
- 좌우 이동과 걷기 애니메이션 중 캐릭터와 아이템이 함께 움직여야 한다.
- 마을, 상점 미리보기, 옷장 미리보기, 랭킹의 상대 위치가 같아야 한다.
- 구매·장착 해제·저장·재접속 후 같은 모습이 유지되어야 한다.
