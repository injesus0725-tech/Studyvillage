# StudyVillage 캐릭터·아이템 규격

최종 갱신: 2026-08-31

> 이 문서의 과거 다중 슬롯·런타임 자동정렬 규격은 폐기되었다. 제작 방향과 인계 기준은 `AVATAR_PRODUCTION_DIRECTION.md`가 최우선이다.

## 공통 기준

- 모든 생산 그래픽의 마스터 좌표계는 **256×256**이다.
- 생산 PNG는 게임에 넣기 전에 마스터 템플릿에서 정렬을 끝낸다. 게임에서는 256×256 이미지를 **0,0에 그대로 겹치기만 한다.**
- **런타임 자동 픽셀 분석·Canvas 재생성·PNG data URL 변환·MutationObserver 기반 재정렬은 금지한다.**
- 기본 캐릭터 변형은 기존 기본 남자·여자와 같은 몸체 크기·목 위치·어깨 위치·발 중앙·캔버스를 사용한다.
- 기본 캐릭터 변형에서 달라지는 것은 머리 모양과 얼굴 표정뿐이다.
- 장착 순서는 `character/outfit/effect/pet`이다.
- 별도 `hair` 슬롯은 사용하지 않는다.
- 구형 슬롯과 테스트 계정 구매 데이터는 보존하지 않는다.

## 최종 마스터 앵커

| 슬롯 | 제작 좌표/크기 | 의미 |
|---|---|---|
| character | 256×256 전신 마스터 | 승인된 기본 몸체를 그대로 사용 |
| outfit | 256×256 / 목선 고정 / 발바닥선 고정 / 기본 의상 must-cover | 몸 전체 외곽이 아니라 착용 접점과 덮기 영역으로 제작 |
| effect | 256×256 / 캐릭터 중심 기준 | 몸 주변 효과용 완성 PNG |
| pet | 256×256 / pet centerX 180 / footY 246 / 시각 높이 약 112 | 캐릭터 오른쪽 다리 가까이에 미리 배치한 완성 PNG |

## 한벌 의상 최종 규격

- **기본 캐릭터의 목선과 발바닥선을 그대로 기준으로 삼는다.**
- 의상 몸체는 기본 캐릭터의 **기본 의상 실루엣을 완전히 덮는 must-cover 영역**보다 조금 넉넉하게 제작한다.
- 어깨·겨드랑이·허리·골반·다리 바깥선에 몇 픽셀 안전 여유를 둬 기본 옷이 삐져나오지 않게 한다.
- 검·지팡이·활·방패·망토·큰 어깨장식·모자 등 외부 장식은 정렬 기준에서 제외한다.
- 외부 장식은 256×256 캔버스 안에서 자유롭게 확장할 수 있다.
- 높은 깃·망토가 목선 위로 올라오는 것은 허용하지만 실제 얼굴 피부·눈·입은 가리지 않는다.
- PNG 전체 폭·높이·불투명 픽셀 중심·medianX·alpha bounds로 몸을 다시 맞추지 않는다.
- PNG의 투명 여백은 256×256 마스터 캔버스의 일부이므로 자동 crop/trim 하지 않는다.
- 기본 남자·여자 및 모든 기본 캐릭터 변형에서 동일한 기준점으로 호환되어야 한다.
- 게임 런타임에 `수호 기사 -3px`, `마법사 1.08배` 같은 개별 CSS 보정을 만들지 않는다. 필요한 수정은 원본 생산 PNG에서 한 번 끝낸다.

## 펫 규격

- 모든 펫은 256×256 완성 PNG 안에서 `centerX 180`, `footY 246`, 시각 높이 약 112px를 기준으로 제작한다.
- 캐릭터 오른쪽 다리에 가까이 붙어 보이고 작은 화면에서 무릎~허벅지 정도 높이로 읽혀야 한다.
- 게임에서 펫마다 별도 `left/right/translate/scale` 값을 만들지 않는다.
- 종별 실루엣을 분명히 다르게 만든다.

## 기본 캐릭터 생산 게이트

- 현재 승인되지 않은 과거 `character-boy-02~05`, `character-girl-02~05` 이미지는 판매용으로 사용하지 않는다.
- 새 기본 캐릭터는 승인된 기본 남자/여자 몸체를 복제한 뒤 **머리·표정만 교체**해서 만든다.
- 새 기본 캐릭터 그림이 실제 제작·검수되기 전에는 상점 상품으로 등록하지 않는다.

## 대량 생산 게이트

1. 256×256 마스터 템플릿에 기본 캐릭터를 깐다.
2. 목선·발바닥선·기본 의상 must-cover 영역을 표시한다.
3. 의상 몸체가 must-cover 영역을 전부 덮는지 확인한다.
4. 검·활·망토 등 외부 장식은 몸 정렬 계산과 분리해서 배치한다.
5. 실제 얼굴 피부·눈·입 침범 여부를 검사한다.
6. 테스트 의상 6벌을 기본 남자·여자에 합성해 상점 화면에서 묶어서 확인한다.
7. 6벌이 통과하면 그 좌표와 덮기 영역을 마스터 규격으로 잠근다.
8. 통과한 256×256 PNG만 `avatar-runtime/production`에 넣는다.
9. 런타임은 픽셀을 읽거나 재정렬하지 않고 0,0 합성만 한다.

## 완료 검사

- 기본 남자·여자에서 의상을 바꿔도 목선과 발바닥선이 변하지 않아야 한다.
- 기본 의상 픽셀이 새 의상 가장자리 밖으로 보이면 실패다.
- 외부 장식 때문에 몸 위치가 밀리면 실패다.
- 얼굴 피부·눈·입이 의상 장식에 가려지면 실패다.
- 게임 런타임 개별 보정값은 0개여야 한다.
- 모든 펫이 완성 PNG에 미리 정렬되어 게임에서는 같은 0,0 합성만 사용해야 한다.
- 좌우 이동과 걷기 애니메이션 중 캐릭터와 아이템이 함께 움직여야 한다.
- 마을, 상점 미리보기, 옷장 미리보기, 랭킹의 상대 위치가 같아야 한다.
- 구매·장착 해제·저장·재접속 후 같은 모습이 유지되어야 한다.
- 상점/옷장 진입 시 런타임 Canvas 픽셀 스캔이 없어야 한다.
