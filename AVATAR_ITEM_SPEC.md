# StudyVillage 캐릭터·아이템 규격

최종 갱신: 2026-08-30

> 이 문서의 과거 다중 슬롯 규격은 폐기되었다. 제작 방향과 인계 기준은 `AVATAR_PRODUCTION_DIRECTION.md`가 최우선이다.

## 공통 기준

- 모든 생산 그래픽의 마스터 좌표계는 **256×256**이다.
- 캐릭터 중심점은 발 중앙이며 마을·꾸미기 미리보기·상점·랭킹에서 같은 상대 좌표를 사용한다.
- **런타임 자동 픽셀 분석·Canvas 재생성·PNG data URL 변환은 금지한다.** 교실 서버와 학생 기기의 안정성을 우선한다.
- 생산 PNG는 게임에 넣기 전에 마스터 템플릿에서 정렬을 끝낸다. 게임에서는 256×256 이미지를 **0,0에 그대로 겹치기만 한다.**
- 기본 캐릭터 변형은 기존 기본 남자·여자와 같은 몸체 크기·목 위치·어깨 위치·발 중앙·캔버스를 사용한다.
- 기본 캐릭터 변형에서 달라지는 것은 머리 모양과 얼굴 표정뿐이다.
- 장착 순서는 `character/outfit/effect/pet`이다.
- 별도 `hair` 슬롯은 사용하지 않는다.
- 구형 슬롯과 테스트 계정 구매 데이터는 보존하지 않는다.

## 최종 마스터 앵커

| 슬롯 | 제작 좌표/크기 | 의미 |
|---|---|---|
| character | 256×256 전신 마스터 | 승인된 기본 몸체를 그대로 사용 |
| outfit | 256×256 / body centerX 128 / footY 246 | 제작 단계에서 몸 중심·발바닥을 맞춘 완성 PNG |
| effect | 256×256 / 캐릭터 중심 기준 | 몸 주변 효과용 완성 PNG |
| pet | 256×256 / pet centerX 180 / footY 246 / 시각 높이 약 112 | 캐릭터 오른쪽 다리 가까이에 미리 배치한 완성 PNG |

## 2026-08-30 최종 시각 기준

대량 생산 전 실제 상점 화면을 기준으로 다음을 최종 QA 기준으로 삼는다.

- `초록 숲 궁수`, `붉은 해적 선장`의 **몸 중심과 발 위치**를 허용 기준으로 사용한다. 약 1~2px 수준의 시각 오차는 허용한다.
- 은빛 수호 기사: 기준보다 왼쪽으로 치우치지 않게 몸 중심을 맞춘다.
- 별자리 마법사: 기준보다 많이 위로 뜨지 않게 발 위치를 맞춘다.
- 푸른빛 과학자: 기준보다 위로 뜨지 않게 발 위치를 맞춘다.
- 달빛 사제: 기준보다 왼쪽으로 치우치지 않게 몸 중심을 맞춘다.
- 해적 선장: 몸 위치는 기준으로 인정하되 목 뒤 장식이 얼굴을 덮지 않게 한다.
- 모든 새 의상은 이 화면 기준의 몸 중심·발 위치·얼굴 보호 영역을 **제작 단계에서** 통과한 뒤 등록한다.

## 한벌 의상 규격

- 얼굴과 머리를 덮지 않는다. 의상 시각 요소는 원칙적으로 목 아래에만 존재한다.
- 얼굴 보호 영역은 기본 캐릭터 얼굴 실루엣을 침범하지 않아야 한다. 높은 깃·망토·장식은 얼굴 뒤쪽까지 올라오지 않게 제작 단계에서 잘라내거나 재배치한다.
- 검·지팡이·활처럼 한쪽으로 튀어나온 장식의 외곽 폭이 아니라 **몸통 중심과 발바닥**을 마스터 앵커에 맞춘다.
- PNG의 투명 여백은 256×256 마스터 캔버스의 일부이므로 자동 crop 하지 않는다.
- 기본 남자·여자 및 모든 기본 캐릭터 변형에서 동일한 기준점으로 호환되어야 한다.
- 게임 런타임에 `수호 기사 -3px`, `마법사 1.08배` 같은 개별 CSS 보정을 만들지 않는다. 필요한 수정은 원본 생산 PNG에서 한 번 끝낸다.

## 펫 규격

- 모든 펫은 256×256 완성 PNG 안에서 `centerX 180`, `footY 246`, 시각 높이 약 112px를 기준으로 제작한다.
- 캐릭터 오른쪽 다리에 가까이 붙어 보이고 작은 화면에서 무릎~허벅지 정도 높이로 읽혀야 한다.
- 게임에서 펫마다 별도 `left/right/translate/scale` 값을 만들지 않는다.
- 종별 실루엣을 분명히 다르게 만든다.

## 기본 캐릭터 생산 게이트

- 현재 승인되지 않은 과거 `character-boy-02~05`, `character-girl-02~05` 이미지는 판매용으로 사용하지 않는다.
- 새 기본 캐릭터는 승인된 기본 남자/여자 몸체를 복제한 뒤 **머리·표정만 교체**해서 만든다.
- 새 기본 캐릭터 그림이 실제 제작·검수되기 전에는 상점 상품으로 등록하지 않는다.

## 대량 생산 게이트

1. 마스터 256×256 템플릿에서 제작한다.
2. 기본 캐릭터와 미리 합성해 몸 중심·발 위치·얼굴 침범 여부를 검사한다.
3. 펫은 같은 합성 화면에서 다리와의 거리·높이를 검사한다.
4. 통과한 256×256 PNG만 `avatar-runtime/production`에 넣는다.
5. 런타임은 픽셀을 읽거나 재정렬하지 않고 0,0 합성만 한다.
6. 여러 아이템은 QA 시트에서 묶어서 확인하고 개별 게임 보정을 반복하지 않는다.

## 완료 검사

- 기본 남자·여자에서 의상을 바꿔도 몸 중심과 발 위치가 변하지 않아야 한다.
- 의상은 게임 런타임 개별 보정값이 0개여야 한다.
- 모든 펫이 완성 PNG에 미리 정렬되어 게임에서는 같은 0,0 합성만 사용해야 한다.
- 좌우 이동과 걷기 애니메이션 중 캐릭터와 아이템이 함께 움직여야 한다.
- 마을, 상점 미리보기, 옷장 미리보기, 랭킹의 상대 위치가 같아야 한다.
- 구매·장착 해제·저장·재접속 후 같은 모습이 유지되어야 한다.
- 상점/옷장 진입 시 런타임 Canvas 픽셀 스캔이 없어야 한다.
