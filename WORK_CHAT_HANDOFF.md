# StudyVillage Work ↔ Chat Handoff Checkpoint

> **이 파일은 Work와 일반 Chat을 오갈 때 작업이 끊기지 않게 하는 최우선 인계 문서입니다.**
> 새 대화에서는 추측으로 이어가지 말고 반드시 `V1_COMPLETION_PLAN.md`와 이 파일을 먼저 읽고 현재 브랜치 HEAD를 확인한 뒤 작업합니다.

## 고정 저장소 / 브랜치
- Repository: `injesus0725-tech/Studyvillage`
- Working branch: `stabilize-touch-buildings-20260820`
- 이 문서를 만든 기준 HEAD: `8b37fccc6b4a5b2193399e62038865aefeaf9cbd`
- 이후 커밋이 생기면 **브랜치 최신 HEAD가 항상 우선**이며, 이 SHA로 되돌아가지 않습니다.

## 작업 재개 절차 — Work/Chat 공통
1. 브랜치 최신 HEAD 확인.
2. `V1_COMPLETION_PLAN.md` 읽기.
3. `WORK_CHAT_HANDOFF.md` 읽기.
4. 직전 커밋 이후 변경 파일 확인.
5. 이미 완료된 캐릭터/꾸미기 작업으로 돌아가지 않음.
6. 사용자가 새로 발견한 오류/요구는 먼저 `V1_COMPLETION_PLAN.md`에 기록한 뒤 적절한 순서에서 구현.
7. 실제 기기 확인 전 항목은 코드 안전망이 있어도 완료 처리하지 않음.
8. 기능 구조 작업이 끝난 뒤 최종 통합 안정화에서 로그인 즉시 멈춤을 최우선 해결.

## 현재 확정된 문제 출제 운영 규칙

### 국어
- 관리자에서 `1학기/2학기 → 단원별 체크박스`로 출제 범위를 선택.
- 체크된 단원의 일반 문제는 **도전관 + 교과 배움터 + 탐험**에 공통 적용하는 것이 최종 요구.
- 책마루용으로 지정된 국어 문제(어려운 낱말, 어휘, 상식, 수수께끼 등)는 책마루에도 사용.
- 같은 단원을 공간마다 교사가 중복 체크하게 만들지 않는다.

### 수학
- 관리자에서 `1학기/2학기 → 단원별 체크박스`로 출제 범위를 선택.
- 체크된 단원의 일반 문제는 **도전관 + 교과 배움터 + 탐험**에 공통 적용하는 것이 최종 요구.
- 수학 놀이터도 **같은 수학 단원 체크를 공유**한다. 수학 놀이터 전용 단원 체크는 만들지 않는다.

### 수학 놀이터 랜덤 생성 규칙
- 현재 별도 서버 생성기(`server/math-practice.js`)가 5문제를 즉석 생성하는 구조이며 일반 문제은행과는 다른 코드 경로다.
- 앞으로 관리자 수학 단원 체크를 이 생성기에도 연결한다.
- 단원을 체크했다고 그 단원의 모든 유형이 수학 놀이터에 나오는 것은 아니다.
- **자동 생성 가능한 숫자/텍스트형 유형만** 수학 놀이터 후보가 된다.
  - 예: 덧셈/뺄셈/곱셈/나눗셈, 분수·소수의 일부 계산/개념, 길이·시간 계산 등.
- **그래프 읽기, 그림/표를 직접 봐야 하는 문제, 시각 자료가 핵심인 도형 유형 등은 단원이 체크돼도 수학 놀이터 랜덤 생성에서 제외**한다.
- 단, 이런 단원의 일반 문제는 체크된 상태라면 도전관/배움터/탐험에서는 정상 출제할 수 있다.
- 지도안/지도서를 입력하면서 단원별 `mathPlaygroundEligible` 성격의 생성 가능 유형을 함께 설계하고 생성 패턴을 늘린다.
- 수학 놀이터 완료 후 오답은 `문제 / 내 답 / 정답 / 짧은 풀이`로 확인하도록 이미 구현됨.

### 사회 + 과학 + 예체능 통합 문제군
- 학생에게는 사회/과학/예체능 문제가 **한 문제군으로 섞여 랜덤 출제**되도록 한다.
- 내부 데이터와 관리자 선택은 과목별로 분리 유지한다.
- 사회: 1학기/2학기 단원별 체크.
- 과학: 1학기/2학기 단원별 체크.
- 음악: 단원별 체크 대신 `음악 이론 문제 출제 ON/OFF` 하나.
- 음악 문제는 계이름, 박자, 음표/쉼표 길이 등 이론 중심.
- 추후 다른 예체능 이론을 넣어도 내부 과목 메타데이터는 보존하고 통합 문제군에 합칠 수 있게 한다.

## 현재 구현 상태에서 반드시 구분할 것
- 기존 관리자 카탈로그는 현재 `교과 배움터 / 책마루 / 탐험` 공간별 체크와 `오늘 사용할 과목` 체크를 갖고 있다.
- **최종 요구는 이를 더 단순화하여 단원 체크 하나가 기본 출제 범위가 되게 하는 것**이다.
- 기존 코드가 이미 있다고 해서 최종 UI 요구가 완료된 것으로 오판하지 않는다.
- 기존 문제 데이터/관리자 수정 이력/문제 고유 ID는 보존한다.
- 지도안 대량 입력 경로 `question-guide-packs.js`와 표준 템플릿은 준비되어 있다.

## 미완료 핵심 버그 — 절대 누락 금지
1. **로그인 즉시 멈춤**
   - GitHub에서 초록으로 표시된 최신 배포본을 실제 실행했을 때 로그인 직후 멈추는 현상.
   - 지금 기능 구조 작업 중에는 보류.
   - 효과음까지 기능 반영 후 최종 통합 안정화의 첫 번째 항목으로 재현/수정/Windows 실기기 확인.
2. **도전관 문제 완료 후 복귀 오류**
   - 사용자가 실제 기기에서 여전히 발생한다고 확인.
   - 코드 안전망이 있어도 실제 패드 확인 전까지 완료 처리 금지.

## 이미 완료되어 되돌아가지 않을 것
- 캐릭터/꾸미기 대량 아이템 및 상점/장착/랭킹/탐험/프로필 기본 연동.
- 캐릭터 저장/재접속/레이어 안정화.
- 탐험 V2 단일 진입 및 다른 활동과의 전면 상태 격리.
- 랜덤 수학 완료 후 오답 다시 보기.
- 문제 데이터 메타데이터/지도안 팩 입력 경로의 기본 구조.

## 앞으로의 고정 진행 순서
1. 관리자 최종 UI/운영 규칙 구현
   - 국어/수학 학기·단원 체크
   - 사회/과학 학기·단원 체크
   - 음악 이론 ON/OFF
   - 사회+과학+예체능 학생 통합 문제군
   - 국어/수학 체크 범위를 도전관+배움터+탐험 공통 적용
2. 수학 놀이터 생성기를 같은 수학 단원 체크와 연결하고, 자동 생성 가능 유형만 확장.
3. 도전관 완료 오류는 코드/실기기 확인 가능한 시점에 해결 완료 판정.
4. 지도안/지도서 첨부 후 실제 문제 대량 입력.
5. 효과음 전체 통합.
6. 최종 통합 안정화: **로그인 즉시 멈춤부터 최우선** → 전체 회귀검사 → iPad → Windows portable.

## 효과음 최종 요구 — 마지막 기능 작업
- 탐험 NPC마다 성격에 맞는 효과음.
- 보물/천사/별/보상: 축하 계열.
- 도둑/악당/감점: 놀림/위험 계열.
- 모든 문제: 문제 등장 / 정답 / 오답 / 완료.
- 메뉴, 책마루, 탐험, 상점 및 웬만한 선택 버튼 클릭음.
- 기존 음소거 설정 유지, 전체 음량/톤 통일.

## 작업 원칙
- 사용자는 `ㄱ` 또는 한 글자로 진행을 지시할 수 있으며, 가능한 한 여러 관련 작업을 묶어서 진행한다.
- 하지만 대형 핵심 파일을 불필요하게 통째로 교체하지 않는다.
- Work와 Chat 중 어느 쪽에서 작업하든 **같은 브랜치 + 같은 두 문서**를 기준으로 한다.
- 새 요구가 생기면 대화 기억에만 두지 말고 작업표에 기록한다.
