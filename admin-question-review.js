/* v1.9 teacher question review panel. Question source stays read-only; review state is stored by the server. */
(()=>{
  const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const headers=json=>{const token=sessionStorage.getItem('studyvillage-admin-token')||'';return token?{Authorization:`Bearer ${token}`,...(json?{'Content-Type':'application/json'}:{})}:{...(json?{'Content-Type':'application/json'}:{})}};
  let loading=false,panel,list,count,summary;
  function ensurePanel(){
    if(panel)return;
    const app=document.querySelector('#admin-app');if(!app)return;
    panel=document.createElement('section');panel.id='question-review-panel';panel.className='panel';panel.innerHTML=`<div class="panel-head"><div><h2>🧩 문제 확인 필요</h2><p>문제 데이터의 형식 이상만 검사합니다. 문제·보기·정답은 자동으로 수정하지 않습니다.</p></div><span id="question-review-count" class="overview-count">0건</span></div><div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px"><button id="question-review-scan">문제 다시 검사</button><span id="question-review-summary" class="status">검사 전</span></div><div id="question-review-list"><p class="empty">문제 검토 기록을 불러오는 중입니다.</p></div>`;
    const anchor=document.querySelector('#score-alert-panel');anchor?.after(panel);if(!anchor)app.prepend(panel);
    list=panel.querySelector('#question-review-list');count=panel.querySelector('#question-review-count');summary=panel.querySelector('#question-review-summary');panel.querySelector('#question-review-scan').addEventListener('click',scan);
    list.addEventListener('click',async e=>{const b=e.target.closest('button[data-review-key]');if(!b)return;b.disabled=true;try{const note=b.dataset.status==='confirmed'?prompt('확인 메모를 남길까요? (선택)','')||'':'';const r=await fetch(`/api/admin/question-reviews/${encodeURIComponent(b.dataset.reviewKey)}/review`,{method:'POST',headers:headers(true),body:JSON.stringify({status:b.dataset.status,note})});if(!r.ok)throw new Error();await loadReviews()}catch{alert('문제 검토 상태를 저장하지 못했습니다.')}finally{b.disabled=false}});
  }
  function extractQuestions(source){const m=source.match(/const questions\s*=\s*(\[[\s\S]*?\]);\s*\n\s*let index=/);if(!m)throw new Error('문제 배열을 찾지 못했습니다.');return Function(`"use strict";return (${m[1]});`)()}
  async function scan(){if(loading)return;loading=true;ensurePanel();summary.textContent='검사 중…';try{const source=await fetch('/library-game.js',{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('문제 파일을 읽지 못했습니다.');return r.text()});const questions=extractQuestions(source),r=await fetch('/api/admin/question-reviews/scan',{method:'POST',headers:headers(true),body:JSON.stringify({sets:[{activityId:'vocabulary',subject:'국어',topic:'어휘',questions}]})}),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.message||'검사 저장 실패');summary.textContent=`${d.summary?.questionCount||questions.length}문제 · 이상 ${d.summary?.issueCount||0}건`;await loadReviews()}catch(err){list.innerHTML=`<p class="empty">문제 검사 실패: ${esc(err?.message||err)}</p>`;summary.textContent='검사 실패';count.textContent='-'}finally{loading=false}}
  async function loadReviews(){ensurePanel();const r=await fetch('/api/admin/question-reviews',{headers:headers(),cache:'no-store'});if(r.status===401)return;const d=await r.json();if(!r.ok||!d.ok)throw new Error(d.message||'검토 기록 읽기 실패');render(d.reviews||[],d.pendingCount||0)}
  function render(rows,pendingCount){count.textContent=`${pendingCount}건`;panel.hidden=false;list.innerHTML='';if(!rows.length){list.innerHTML='<div class="attention-clear">✅ 현재 자동 검사에서 확인 필요한 문제 형식이 발견되지 않았습니다.</div>';return}rows.forEach(r=>{const article=document.createElement('article');article.className='attention-card';const done=r.status==='confirmed'?'확인 완료':r.status==='dismissed'?'문제 없음':'확인 필요';article.innerHTML=`<div class="attention-card-head"><strong>${r.severity==='error'?'🔴':'🟡'} ${esc(r.activityId)} · ${r.questionNumber?`${r.questionNumber}번`:'전체'}</strong><span>${done}</span></div><p>${esc(r.message)}</p><small>${esc(r.subject||'')}${r.topic?` · ${esc(r.topic)}`:''} · ${esc(r.code)}${r.teacherNote?` · 메모: ${esc(r.teacherNote)}`:''}</small><div style="margin-top:8px;display:flex;gap:6px;flex-wrap:wrap"><button data-review-key="${esc(r.reviewKey)}" data-status="confirmed">확인 완료</button><button data-review-key="${esc(r.reviewKey)}" data-status="dismissed">문제 없음</button><button data-review-key="${esc(r.reviewKey)}" data-status="pending">다시 확인</button></div>`;list.appendChild(article)})}
  async function maybeLoad(){ensurePanel();const app=document.querySelector('#admin-app');if(!app||app.hidden)return;try{await loadReviews()}catch{} }
  window.addEventListener('load',()=>{ensurePanel();setTimeout(async()=>{await maybeLoad();try{const r=await fetch('/api/admin/question-reviews',{headers:headers(),cache:'no-store'});if(r.status!==401)await scan()}catch{}},350)});
  document.querySelector('#refresh-button')?.addEventListener('click',()=>setTimeout(maybeLoad,250));
  new MutationObserver(()=>maybeLoad()).observe(document.documentElement,{subtree:true,attributes:true,attributeFilter:['hidden']});
})();
