# StudyVillage V1 Release Checklist

이 문서는 `V1_COMPLETION_PLAN.md`의 긴 개발 기록과 별도로, **V1 배포 직전에 무엇이 실제로 남았는지**만 빠르게 확인하기 위한 릴리스 체크리스트입니다.

## 1. 코드 완료 — 다시 개발할 필요 없음

아래 항목은 현재 브랜치의 회귀검사와 코드 안전망까지 연결된 상태입니다. 실기기 확인에서 문제가 재현되지 않는 한 다시 구조를 뜯지 않습니다.

- [x] 관리자 과목/학기/단원/문제 ON·OFF → 학생이 다음 활동 진입 전에 최신 설정 재조회
- [x] 단원 OFF→ON 시 원본 문제은행 복구 및 보강팩/지도안팩 로딩 순서 보장
- [x] 수학 놀이터가 관리자 수학 단원 체크를 공유하고 자동 생성 가능한 유형만 출제
- [x] 핵심 학습 활동은 교사가 허용한 새 완료마다 XP 지급
- [x] 별에는 활동별 정상 새 완료를 막는 공통 일일 총획득 상한 없음
- [x] 동일 submission 재전송은 XP·별 중복 지급 없이 기존 결과 재사용
- [x] 도전관 완료 결과에서 `마을로 돌아가기`가 안전한 닫기 경로를 재사용
- [x] 책마루 완료 결과에서 바로 마을 복귀
- [x] 책마루 피드백 후 자동 이동 없음, 명시적 `다음 문제`/`결과 보기` 버튼 사용
- [x] 상점과 내 옷장 패널 분리
- [x] 구매 아이템 즉시 장착 경로와 재접속 복원 경로 연결
- [x] 구매 아이템을 `착용 안 함`으로 해제했을 때 null 상태도 서버에 저장
- [x] 장비 저장 두 레이어를 완전 쌍으로 최대 1회 재시도
- [x] 방향키 UI 제거, 마을 영역 탭 이동 사용
- [x] 포인터/터치 이중 실행을 일으키는 전역 `touchend` 경로 제거
- [x] 미션·도감·꾸미기·랭킹·건물·활동 오버레이의 Escape/뒤로가기 우선순위 격리
- [x] 탐험 수수께끼 독립 메뉴 제거, 공통 문제 풀에 혼합
- [x] 공통 효과음 관리자와 주요 버튼/정답/오답/보상/NPC 효과음 연결
- [x] 국어 14개 단원 최소 8문제, 예체능 확충, 책마루 다양화, 수학 12단원 보강
- [x] 새 보강팩이 학생/관리자/Windows portable `assets/**/*` 패키징 경로에 포함되도록 검사
- [x] 관리자가 새 학생 가입을 허용/잠금할 수 있고, 잠금 중에도 기존 학생 로그인은 계속 허용
- [x] V1 release readiness guard가 `npm verify`의 runtime-critical 검사 경로에 포함

## 2. 실기기 확인만 남음 — 코드 미완료가 아님

아래는 새 배포본에서 실제 행동만 확인합니다. 이상이 없으면 체크하고 V1을 닫습니다.

- [ ] **관리자 단원 즉시 반영**: 학생 로그인 유지 → 관리자에서 국어/수학 단원 하나 OFF → 학생이 다음 배움터/탐험/책마루 진입 → OFF 단원 미출제 확인 → 다시 ON → 재출제 확인
- [ ] **도전관 완료 복귀**: 마지막 문제 정답 → 결과 확인 → `마을로 돌아가기` 1회 → 마을 정상 복귀 → 바로 다른 건물 진입
- [ ] **책마루 완료 복귀**: 7문제 완료 → 결과 → `마을로 돌아가기` → 마을 정상 복귀 → 바로 다른 활동 진입
- [ ] **구매→장착→해제→재접속**: 상점 구매 → 즉시 장착 → 로그아웃/재로그인 후 유지 → `착용 안 함` → 재로그인 후 다시 나타나지 않음
- [ ] **iPad 연속 터치**: 건물/상점/옷장/미션/도감 버튼을 빠르게 눌러도 패널 중복·화면 겹침·이중 시작 없음
- [ ] **iPad 뒤로가기/Escape 계열**: 열린 최상단 패널 하나만 닫히고 배경 패널까지 함께 닫히지 않음
- [ ] **15명 동시 기본 회귀**: 로그인 → 배움터/책마루/수학/탐험 각각 최소 1회 → 저장·별·XP·마을 복귀 확인
- [ ] **신규 가입 잠금**: 관리자에서 가입 잠금 → 기존 학생 로그인 성공 → 등록되지 않은 이름은 생성되지 않고 안내 표시 → 다시 허용 후 새 학생 생성 성공

## 3. 실행 검증

자동 검증과 Windows 빌드는 완료했습니다. 마지막 항목만 교실 PC에서 직접 확인합니다.

- [x] `npm run verify`
- [x] classroom regression 실행
- [x] GitHub Actions Windows portable build 및 산출물 업로드
- [ ] 새 portable 실행 후 학생/관리자 화면의 최신 빌드 확인

## 4. V1 이후 또는 지도서 입력과 함께 계속 확장

아래는 V1 출시를 막는 오류가 아니라 콘텐츠 확장 항목입니다.

- [x] 사회 핵심 개념 문제를 4개 단원 각 20문제 이상으로 1차 확충
- [x] 과학 핵심 개념 문제를 8개 단원 각 15문제 이상으로 1차 확충
- [ ] 실제 지도서 기반 사회·과학 문제를 단원별로 계속 추가
- [ ] 국어 지도서 어려운 낱말·책마루 어휘 지속 추가
- [ ] 수수께끼·상식·교과 책마루 문제 지속 추가
- [ ] 실제 지도안/지도서 기반 수학·사회·과학 문제 팩 추가
- [ ] 실제 사용 피드백 기반 헤어·옷·액세서리 등 캐릭터 아이템 지속 추가

## V1 종료 판단

다음 세 조건을 모두 만족하면 V1 안정화 작업을 종료합니다.

1. 위 **실기기 확인 8개**에서 치명적 오류가 재현되지 않음.
2. `npm run verify`와 Windows portable build가 실제 환경에서 통과함.
3. 교실에서 학생 15명 기본 회귀 사용 중 로그인 멈춤·저장 실패·복귀 먹통·패널 겹침이 재현되지 않음.

문제은행 추가는 이후에도 데이터 팩만 확장할 수 있으므로 V1 출시를 무기한 미루는 사유로 보지 않습니다.
