# Studyvillage 문제 세트 표준 템플릿

새 문제를 추가할 때는 기존 `question-data.js` 구조를 유지하고 아래 항목을 빠뜨리지 않는다.

```js
sampleSet: {
  activityId: 'math-fractions',
  subject: '수학',
  topic: '분수',
  questions: [
    {
      prompt: '문제 문장',
      options: ['보기1', '보기2', '보기3', '보기4'],
      answer: 0
    }
  ]
}
```

## 필수 규칙

- `activityId`: 영문 소문자, 숫자, 하이픈만 사용한다. 한 번 정한 ID는 점수 기록과 연결되므로 쉽게 바꾸지 않는다.
- `subject`: 학생 기록 화면에서 과목별 점수를 묶는 기준이다. 예: `국어`, `수학`, `사회`, `과학`.
- `topic`: 같은 과목 안에서 학생이 점수를 구분해 볼 주제다. 예: `어휘`, `분수`, `우리 고장`.
- `questions`: 반드시 배열이어야 한다.
- 객관식은 `options`와 `answer`를 사용하고, `answer`는 보기의 0부터 시작하는 번호다.
- 문제 데이터는 프로그램이 자동으로 고치지 않는다. 감사에서 이상이 발견되면 교사 검토 후 수정한다.
- 지도서 기반 문제를 추가할 때도 원문 취지와 정답 근거를 확인하고, 과목·주제 메타데이터를 함께 넣는다.

## 추가 원칙

- 새 활동을 만들면 도전 횟수 정책(`1회 / N회 / 무제한`)도 관리자에서 확인한다.
- 새 활동 점수는 가능하면 공통 `/api/player/me/activity` 저장 경로를 사용한다.
- 새 문제 세트는 `question-metadata-selftest.js`와 기존 문제 감사 검사를 통과해야 한다.
- 학생 기록 화면은 문제 세트의 `activityId / subject / topic`을 자동으로 읽어 과목·주제별로 표시한다.
