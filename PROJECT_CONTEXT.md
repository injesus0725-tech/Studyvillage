# Studyvillage Project Context

> 새 대화에서도 프로젝트 방향과 중요한 결정을 빠르게 복구하기 위한 연속성 기록. 실제 구현 상태는 항상 GitHub `main`을 우선한다.

## 기준점
- 프로젝트: **우리 학습마을 (Studyvillage)**
- 기준 버전: **v0.9.12**
- 기준 브랜치: `main`
- 다음 개발은 v0.9.12를 훼손하거나 옛 버전으로 되돌리지 않고 누적한다.

## 프로젝트 목표
초등 교실에서 학생들이 태블릿/브라우저로 캐릭터를 움직여 마을의 건물별 학습 활동과 미니게임을 수행하고, 결과가 개인 기록과 공통 성장/보상 체계에 연결되는 **교실용 학습 RPG**. 게임 자체보다 학습 활동을 재미있는 마을 탐험 구조 안에 넣는 것이 핵심이다.

## 핵심 운영 결정
- 선생님 교실 PC = Express + SQLite 서버
- 학생은 같은 교실/학교 네트워크에서 QR 접속
- Electron Windows portable 앱을 기본 교사용 실행 형태로 유지
- DB는 앱과 분리된 사용자 데이터 영역에 저장하여 업데이트 시 학생 기록 보존
- 관리자 백업/복원 유지
- 외부 클라우드/Firebase는 기본 운영이 아닌 보조 가능성
- **1.0은 억지로 맞추는 마감점이 아니다. 버전 숫자보다 실제 교실 안정성, 복구 가능성, 데이터 안전성을 우선한다.**

## 구현 흐름 요약
- v0.2.x: 마을 이동, 모바일 조작, 충돌, NPC, 도전관 수수께끼
- v0.5.x: Electron 실행기, 로컬 서버/SQLite, 로그인, 관리자, QR, 백업/복원
- v0.8.0~0.8.3: 큰 마을/구역/건물 내부/책마루 어휘
- v0.8.4~0.8.6: 활동별 기록, 공통 XP, 수수께끼 기록 호환 연결
- v0.8.7~0.8.9: 교사용 학생별 기록, 학급 현황, 살펴볼 학생
- v0.9.0: 꾸미기 상점 실제 오픈
- v0.9.1~0.9.5: 교실 시작 점검, 학생 안내, 연결 상태 안내, 오프라인 점수 활동 시작 방지
- v0.9.6~0.9.11: 오류 자동 수집, 반복 묶기, 우선순위, ChatGPT 진단 파일, 서버/네트워크 스냅샷

### v0.9.12 — 학생 세션 자동 복구
- 교실 서버 로그인 토큰과 학생 이름을 `sessionStorage`에 보관한다.
- 학생이 같은 브라우저 탭에서 새로고침하거나 화면이 순간적으로 다시 로드되더라도 서버 세션이 살아 있으면 `/api/player/me`로 세션을 확인한 뒤 **비밀번호를 다시 입력하지 않고 자동으로 마을에 재입장**한다.
- 서버가 재시작되어 세션 토큰이 무효가 되었으면 자동 복구를 시도하지 않고 기존 로그인 화면을 사용한다.
- 학생 비밀번호 자체는 저장하지 않는다.
- 점수, XP, 활동 기록 계산은 변경하지 않는다.

## 아이디어/보류 백로그
### 정확한 실시간 학생 접속 현황
- 교사 관리자에서 `현재 접속 중 / 방금 연결 끊김 / 미접속`을 정확히 확인할 수 있는 기능을 구현한다.
- 로그인 시각만으로 추정하지 않고 학생 기기가 서버에 주기적으로 heartbeat를 보내는 방식으로 설계한다.
- 예: 15초마다 신호, 최근 약 45초 안에 신호가 있으면 접속 중으로 판단.
- 오류 로그나 활동 로그를 heartbeat 용도로 남용하지 않는다.

### 동시 접속용 실시간 마을 이벤트 방송
- `광철이가 유령에게 별 7개를 획득했습니다!` 같은 축하 알림은 **같은 시점에 같은 교실 서버에 접속 중인 학생들에게만** 실시간으로 보여주는 구조를 추후 구현한다.
- 실제 획득/보상 기록은 SQLite에 남기고, 실시간 화면 알림은 현재 접속 중인 학생에게만 잠깐 표시한다.
- 늦게 접속한 학생에게 과거 알림을 몰아서 보여주지 않는다.
- 로컬 단독 모드나 교실 서버 미접속 상태에서는 전체 방송을 띄우지 않는다.
- 전체 방송 대상은 레벨업, 희귀 보상, 특별 업적, 보스/유령 처치처럼 축하 가치가 큰 사건 위주로 제한하고 일반 문제 정답까지 방송하지 않는다.
- 정확한 heartbeat/동시접속 구조를 만든 뒤 같은 실시간 서버 계층 위에 이벤트 방송을 연결한다.

### 학생 캐릭터 성장/레벨업
- XP 기반 레벨업을 외형, 칭호, 해금 요소, 장식, 이동/표현 요소와 연결 가능.
- 고정 XP 레벨업은 장기 운영 시 너무 빠를 수 있으므로 초기에는 쉽게, 높은 레벨에서는 점차 천천히 오르는 RPG식 곡선을 추후 설계한다.

### 학습 활동별 도전 횟수 설정
- 교사가 활동마다 허용 도전 횟수를 설정할 수 있게 한다.
- 수수께끼/평가형은 1회, 연습형은 여러 번 또는 제한 없음 등으로 구분한다.
- 학생별 남은 횟수, 교사 초기화/추가 허용, XP 중복 획득 방지를 함께 설계한다.

### 교과별 문제 유형/활동 방향
- 수학: 랜덤 출제형 반복 연습, 주관식 입력형 우선.
- 국어: 객관식 선택형 우선.
- 1회 평가형과 반복 연습형을 같은 활동 엔진 설정값으로 구분하는 방향 검토.

### 교사의 학생 데이터 수정/복구
- 오류, 잘못된 기록, 중복 계정, 실수 입력에 대비해 관리자 화면에서 학생 데이터를 안전하게 수정/복구하는 기능 제공.
- 후보: 이름/계정, XP, 점수, 활동별 시도/성적, 해금/장착 상태.
- 파생값은 원본 XP/활동 기록과 일관성을 유지하고, 위험한 수정은 확인 절차와 가능하면 자동 백업/변경 이력/되돌리기를 제공한다.

### 튕김/비정상 종료 복구와 활동 체크포인트
- 이미 SQLite에 저장된 계정·점수·XP·완료 기록은 계속 보존한다.
- 진행 중 활동은 문제 번호, 현재 점수, 응답 상태 등 최소 체크포인트를 임시 저장해 `하던 활동을 이어서 할까요?` 흐름으로 복구하는 기능을 추후 구현한다.
- 체크포인트는 완료 기록/XP와 분리하고 활동 완료 시 삭제하여 중복 보상을 막는다.

### 오류 리포터 추가 발전
- 오류 그룹별 해결 여부/메모, 오래된 오류 자동 정리, 서버/Electron 실행 오류까지 같은 진단 패키지로 묶는 기능을 검토한다.

### 상점의 장기 발전 가능성
- 현재 상점은 해금 아이템 확인/꾸미기 공간.
- 향후 학습 보상 포인트, 아이템 구매, 한정 장식, 교사 지급 보상 등으로 확장 가능.
- 학습보다 아이템 경쟁이 중심이 되지 않도록 교육적 보상 구조를 우선한다.

## 앞으로 기능 추가 원칙
1. 기존 기능을 지우기보다 누적 확장.
2. 이전 정상 기능을 깨뜨리지 않기.
3. 건물은 가능한 한 실제 학습/성장 기능으로 연결.
4. 새 활동은 공통 학생 계정, 활동 기록, XP에 연결.
5. PC와 태블릿/모바일 함께 고려.
6. 서버/DB 복잡성을 교사에게 노출하지 않기.
7. 외부 인터넷 없이 내부망 핵심 기능 동작.
8. 앱 업데이트와 학생 데이터 분리.
9. GitHub `main`이 구현 상태의 최종 기준.
10. 중요한 결정은 코드와 이 문서를 함께 갱신.
11. 구현하지 않은 아이디어도 백로그에 기록.
12. 1.0이라는 숫자보다 실제 교실 안정성과 데이터 안전을 우선한다.
13. 학생이 기술적인 오류 처리 절차를 직접 수행하게 하지 않는다.
14. 오류 진단은 교사가 직접 해석하기보다 ChatGPT에 전달하기 좋은 형태로 수집/내보낸다.
15. 실시간 접속 상태처럼 정확성이 중요한 기능은 임시 추정값으로 표시하지 않고 필요한 서버 구조를 갖춘 뒤 구현한다.

## 새 채팅 복구 절차
1. `PROJECT_CONTEXT.md` 확인
2. GitHub `main` 최신 버전 확인
3. 필요할 때만 이전 대화 특정 결정 검색
4. 최신 버전부터 누적 개발
5. 새 결정/보류 아이디어 기록

## 현재 이어갈 지점
**v0.9.12 완료 상태에서 다음 버전(v0.9.13) 설계를 시작한다.**

핵심 축:
`교실 PC 서버/SQLite` → `학생 로그인/기록` → `RPG 마을/건물` → `학습활동/XP` → `교사용 현황` → `꾸미기 상점` → `교실 시작/학생 안내` → `연결 안정화` → `오류 자동 수집/ChatGPT 진단` → `학생 세션 자동 복구`
