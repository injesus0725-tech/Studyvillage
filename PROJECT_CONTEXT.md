# Studyvillage Project Context

> 이 문서는 새 ChatGPT 대화에서도 프로젝트의 방향과 중요한 결정을 빠르게 복구하기 위한 연속성 기록이다.
> 코드의 실제 현재 상태는 항상 `main` 브랜치를 우선하며, 이 문서는 코드에 드러나지 않는 설계 의도와 운영 원칙을 보완한다.

## 기준점

- 프로젝트: **우리 학습마을 (Studyvillage)**
- 기준 버전: **v0.8.9**
- 기준 브랜치: `main`
- 다음 개발은 **v0.8.9를 훼손하거나 옛 버전으로 되돌리지 않고 누적해서 진행**한다.

## 프로젝트 목표

Studyvillage는 초등 교실에서 학생들이 태블릿/브라우저로 접속해 캐릭터로 마을을 돌아다니고, 건물별 학습 활동과 미니게임을 수행하며, 그 결과가 학생 개인 기록과 공통 성장/보상 체계에 연결되는 **교실용 학습 RPG**이다.

게임 자체가 목적이 아니라 **학습 활동을 재미있는 마을 탐험 구조 안에 넣는 것**이 핵심이다.

## 가장 중요한 운영 결정

### 1. 교실 PC가 서버
- **선생님 교실 컴퓨터 = 서버**
- 학생은 같은 학교/교실 네트워크에서 QR로 접속
- 학생 기록은 선생님 PC에 저장
- 기본 서버는 Node/Express 계열 + SQLite
- 가정 접속을 기본 목표로 하지 않는다.
- Firebase는 주 운영 방식이 아니라 선택 가능한 보조/대체 경로다.

### 2. 독립 실행형 교사용 앱
목표 흐름: `Studyvillage.exe 실행 → 교실 서버 자동 시작 → QR 표시 → 학생 접속 → 이름+비밀번호 로그인 → SQLite 저장`.
Electron 기반 교사용 실행기와 Windows portable EXE 패키징 기반을 유지한다. 실제 DB는 사용자 데이터 영역의 별도 `data/studyvillage.db`에 저장해 앱 버전 교체와 학생 데이터 수명을 분리한다.

### 3. 학생 데이터는 버전 업데이트와 분리
- 새 버전 배포 때문에 학생 기록이 초기화되면 안 된다.
- 관리자 백업/복원 구조를 유지한다.
- 학생 계정 인증 정보와 활동 기록은 교사용 데이터로 취급한다.

## 초기 플레이 구조에서 확정된 것

### v0.2.x
- 브라우저 시작 화면 → 마을 광장 → 학생 캐릭터 이동
- 키보드 방향키/WASD 및 모바일 방향 버튼
- 맵 가장자리/오브젝트 충돌과 대각선 이동
- 안내 NPC 마을지기와 Space/모바일 상호작용
- 대화 중 이동 잠금
- 도전관 수수께끼 10문제, 피드백/설명/점수/결과/다시하기

## 데이터/서버 설계
- 게임 화면과 저장 구현을 강하게 결합하지 않고 데이터 서비스 계층을 분리한다.
- 기본 운영은 로컬 서버 + SQLite다.
- 학생 식별/로그인과 활동 기록은 XP 및 활동별 기록의 기반이다.

## v0.5.x 독립 실행 기반
- Electron 교사용 실행기
- Express + SQLite 서버 자동 시작
- 학생 로그인/기록 저장
- 교사용 관리자 인증 및 학생 계정 관리
- 네트워크 어댑터별 QR 자동 생성
- 관리자 백업/복원
- Windows portable EXE 및 GitHub Actions 빌드 기반

## v0.8.x 개발 기록

### v0.8.0 — 큰 마을/카메라
큰 마을과 스크롤 카메라 기반을 추가했다.

### v0.8.1 — 마을 구역화
도로, 중앙광장, 구역 단위 확장 기반을 추가했다.

### v0.8.2 — 건물 내부
건물 입구/내부 전환 시스템을 추가해 건물을 실제 학습 공간으로 발전시킬 기반을 만들었다.

### v0.8.3 — 책마루 어휘 활동
책마루 내부에 낱말 뜻 맞추기 미니게임을 연결했다.

### v0.8.4 — 활동 기록 + 공통 XP
- 개별 학습 활동 결과를 활동별 기록으로 저장
- 책마루 어휘 활동 결과 연결
- 활동 보상을 공통 XP 체계에 연결

### v0.8.5 — 내 기록 활동별 성적표
- 학생 내 기록 화면에 활동별 카드
- 도전 횟수/최고/최근/누적 점수
- 새 activity_records 활동 자동 확장

### v0.8.6 — 도전관 수수께끼 기록 연결
- 오래된 전체 수수께끼 기록을 `riddle` 활동 카드로 읽는 호환 브리지
- XP 중복 지급을 피하기 위해 별도 activity API로 재저장하지 않는다.
- 향후 완전 통합 시 데이터 마이그레이션 여부를 별도 논의한다.

### v0.8.7 — 교사용 학생별 활동 기록
- 학생별 `활동 보기`
- 레벨/XP/칭호/배지 및 활동별 통계 조회
- 조회 기능만 추가하고 XP 계산은 변경하지 않음

### v0.8.8 — 교사용 우리 반 활동 현황
- 활동별 참여 학생 수/참여율/총 도전/최근 평균/학급 최고
- 책마루와 도전관 기록을 동일한 집계 화면에서 조회
- 새로운 활동도 학생 activities를 기준으로 확장 가능

### v0.8.9 — 교사용 살펴볼 학생
- 관리자 첫 화면에 **살펴볼 학생** 영역 추가
- 현재 기준은 `7일 이상 미접속`, `2회 이상 로그인했지만 학습 활동 기록 없음`, `최근 점수가 50점 미만인 활동 존재`다.
- 이것은 학생을 평가하거나 자동 판정하는 기능이 아니라 **교사가 기록을 한 번 더 살펴보도록 돕는 참고 신호**다.
- 해당 카드에서 바로 학생 활동별 기록을 열 수 있다.
- 기준값은 실제 교실 사용 후 조정할 수 있으며, 현재 XP/레벨 계산이나 학생 기록 저장 방식에는 영향을 주지 않는다.

## 아이디어/보류 백로그 — 지금 당장 구현하지 않아도 잊지 말 것

### 학생 캐릭터 성장/레벨업
- 학습 활동과 XP를 통한 캐릭터 레벨업을 발전시키고 싶다.
- 숫자뿐 아니라 외형, 칭호, 해금 요소, 장식, 이동/표현 요소와 연결할 가능성을 열어둔다.
- 구체적인 보상 방식은 1.0 이후 사용자와 함께 논의한다.

### 경험치/레벨 곡선 재설계 필요
- 단순 고정 XP 레벨업은 장기 운영 시 너무 빨리 오를 가능성이 있다.
- RPG처럼 초기에는 비교적 쉽게, 높은 레벨에서는 점차 천천히 오르는 성장 곡선을 고려한다.
- 공식 수식/테이블은 아직 확정하지 않는다.
- 1.0 이후 활동당 XP, 한 학기 예상 활동 횟수, 목표 최고 레벨을 보고 밸런스를 설계한다.
- 중간 단계에서 XP 구조를 크게 바꾸지 않는다.

### 운영 원칙: 아이디어를 구현보다 먼저 잃지 않기
- 사용자가 개발 중간에 나중에 하고 싶은 기능/변경 아이디어를 말하면 코드에 즉시 반영하지 않더라도 이 백로그에 기록한다.
- 1.0까지 핵심 루프와 안정성을 우선한다.
- 1.0 이후 백로그를 사용자와 하나씩 검토한다.
- 보류 아이디어와 확정 설계는 구분한다.

## 앞으로 기능을 추가할 때 지킬 원칙
1. 기존 기능을 지우기보다 누적 확장한다.
2. 버전 하나를 올릴 때 기존 정상 기능을 깨뜨리지 않는다.
3. 건물은 가능한 한 학습 활동으로 연결되는 공간으로 발전시킨다.
4. 새 활동은 공통 학생 계정, 활동 기록, XP 체계에 연결한다.
5. PC와 학생 태블릿/모바일 조작을 함께 고려한다.
6. 서버/DB 운영 복잡성을 교사에게 노출하지 않는다.
7. 외부 인터넷 없이 교실 내부망에서 핵심 기능이 동작하는 방향을 기본으로 한다.
8. 앱 업데이트와 학생 데이터를 분리한다.
9. GitHub `main` 최신 코드가 구현 상태의 최종 기준이다.
10. 중요한 설계 결정은 코드와 이 문서를 함께 갱신한다.
11. 구현하지 않은 아이디어도 백로그에 기록한다.

## 새 채팅에서의 권장 복구 절차
1. `PROJECT_CONTEXT.md` 확인
2. GitHub `main` 최신 커밋/버전 확인
3. 필요할 때만 이전 대화의 특정 결정 검색
4. 최신 버전부터 누적 개발
5. 중요한 새 결정/보류 아이디어 기록

## 현재 이어갈 지점

**v0.8.9 완료 상태에서 다음 버전(v0.9.0) 설계를 시작한다.**

핵심 축:
`교실 PC 서버/SQLite` → `학생 로그인/기록` → `독립 실행 앱` → `RPG 마을` → `건물 내부` → `책마루 어휘` → `공통 XP` → `학생 활동별 기록` → `교사용 학생별 기록` → `우리 반 활동 현황` → `살펴볼 학생`
