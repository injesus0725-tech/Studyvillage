# Studyvillage Project Context

> 새 대화에서도 프로젝트 방향과 중요한 결정을 빠르게 복구하기 위한 연속성 기록. 실제 구현 상태는 항상 GitHub `main`을 우선한다.

## 기준점
- 프로젝트: **우리 학습마을 (Studyvillage)**
- 기준 버전: **v0.9.27**
- 기준 브랜치: `main`
- 다음 개발은 v0.9.27을 훼손하거나 옛 버전으로 되돌리지 않고 누적한다.

## 프로젝트 목표
초등 교실에서 학생들이 태블릿/브라우저로 캐릭터를 움직여 마을의 건물별 학습 활동과 미니게임을 수행하고, 결과가 개인 기록과 공통 성장/보상 체계에 연결되는 **교실용 학습 RPG**. 게임 자체보다 학습 활동을 재미있는 마을 탐험 구조 안에 넣는 것이 핵심이다.

## 핵심 운영 결정
- 교사 PC = Express + SQLite 교실 서버.
- 학생은 같은 교실/학교 네트워크에서 QR 접속.
- Electron Windows portable 앱을 기본 교사용 실행 형태로 유지.
- DB는 앱과 분리된 사용자 데이터 영역에 저장하여 업데이트 시 학생 기록 보존.
- 관리자 백업/복원 유지.
- 외부 클라우드는 기본 운영이 아닌 보조 가능성.
- **1.0은 억지로 맞추는 마감점이 아니다. 버전 숫자보다 실제 교실 안정성, 복구 가능성, 데이터 안전성을 우선한다.**

## 구현 흐름 요약
- v0.2.x: 마을 이동, 모바일 조작, 충돌, NPC, 도전관 수수께끼.
- v0.5.x: Electron 실행기, 로컬 서버/SQLite, 로그인, 관리자, QR, 백업/복원.
- v0.8.0~0.8.3: 큰 마을/구역/건물 내부/책마루 어휘.
- v0.8.4~0.8.6: 활동별 기록, 공통 XP, 수수께끼 기록 호환 연결.
- v0.8.7~0.8.9: 교사용 학생별 기록, 학급 현황, 살펴볼 학생.
- v0.9.0: 꾸미기 상점 실제 오픈.
- v0.9.1~0.9.5: 교실 시작 점검, 학생 안내, 연결 상태 안내, 오프라인 점수 활동 시작 방지.
- v0.9.6~0.9.11: 오류 자동 수집, 반복 묶기, 우선순위, ChatGPT 진단 파일, 서버/네트워크 스냅샷.
- v0.9.12: 학생 세션 자동 복구.
- v0.9.13: heartbeat 기반 실시간 접속 현황.
- v0.9.14: 실시간 마을 이벤트 표시층.
- v0.9.15: 알림/문제 UI 레인 분리.
- v0.9.16: 현재 접속 학생만 대상으로 하는 실시간 서버 방송 통로.
- v0.9.17: 학생 조작/화면 복귀 시 presence 즉시 갱신 + 8초 쿨다운.
- v0.9.18: 동일 알림 10초 중복 억제 + 알림 대기열 최대 5개.
- v0.9.19: 교사용 방송 중복 전송·더블클릭·IME 오발송 방지.
- v0.9.20: 방송 전 현재 수신 대상 인원 미리보기.
- v0.9.21: 교사용 방송 초안 자동 복구, 빈 전송 방지, 글자 수 표시.
- v0.9.22: 교사 접속 현황 조회를 화면이 보일 때만 수행하도록 최적화.
- v0.9.23: 방송 수신 예상 인원 조회도 숨김 탭에서 중단하도록 최적화.
- v0.9.24: 학생 heartbeat/실시간 알림 타이머를 오프라인·숨김 상태에서 완전히 중단.
- v0.9.25: 학생 서버 연결 상태 확인도 실제 게임 화면을 볼 때만 동작하도록 최적화.
- v0.9.26: 서버 health 확인에 3초 timeout 적용.

### v0.9.27 — 로그인/세션 복구 무한 대기 방지
- `/api/login` 실제 로그인 요청과 `/api/player/me` 세션 복구 조회에도 **5초 timeout**을 적용한다.
- 서버 프로세스가 살아 있으나 요청 처리만 멎는 비정상 상태에서도 학생 화면이 무한 대기하지 않는다.
- health 확인은 기존 3초 timeout을 유지한다.
- 공통 `timedFetch`를 사용해 요청 제한 로직을 한 곳에서 관리한다.
- 로그인 timeout은 `server-timeout`으로 구분해 이후 학생 안내/진단 기능에서 식별 가능하게 한다.
- 기존 비밀번호 검증, 세션 저장, 로컬 fallback, XP, 점수, SQLite 구조는 변경하지 않는다.

## 실시간 마을 이벤트 원칙
- `광철이가 유령에게 별 7개를 획득했습니다!` 같은 축하 알림은 **그 순간 같은 교실 서버에 접속 중이던 학생에게만** 보여준다.
- 실제 보상 기록과 화면 알림은 분리한다. 실제 기록은 SQLite, 실시간 알림은 서버 메모리의 짧은 이벤트로 처리한다.
- 늦게 접속한 학생에게 과거 알림을 몰아서 보여주지 않는다.
- 일반 문제 정답처럼 사소한 행동은 전체 방송하지 않고, 레벨업·희귀 보상·특별 업적·보스/유령 처치처럼 축하 가치가 큰 사건 위주로 제한한다.
- 알림은 화면 **상단 전용 레인**, 문제·선택지·정답 피드백은 학습 패널 영역을 사용해 겹치지 않는다.
- 반복 동일 알림은 억제하고 대기열을 제한해 수업을 방해하지 않게 한다.

## 아이디어/보류 백로그
### 모험 보상 확정/임시 보상
- 모험 중 점수/별/재화는 **임시 진행값**으로 관리하고 정상 완료 후 서버 검증을 거쳐 SQLite 실제 보유값으로 확정한다.
- 긴 모험은 현재 단계, 임시 점수, 임시 별 정도만 체크포인트로 저장할 수 있다.
- 임시 별/재화는 완료 승인 전에 상점 구매 등에 사용할 수 없다.
- 완료 요청은 식별자/완료 상태를 사용해 중복 지급되지 않도록 idempotent하게 설계한다.
- 생각의 숲·도전의 산은 정답 진행에 따른 별을 `임시 별`로만 표시하고 체크포인트에 보관한다. 마지막 문제 뒤 서버가 활동별 가능한 점수와 완료 식별자를 검증한 경우에만 활동 기록·XP·별 장부를 한 트랜잭션으로 확정하며, 같은 완료 식별자의 별은 다시 지급하지 않는다.

### 실시간 이벤트 자동 연결
- 레벨업, 희귀 아이템, 특별 업적, 유령/보스 보상 발생 시 현재 실시간 방송 통로에 자동 연결한다.
- 학생 이름과 축하할 만한 결과만 최소한으로 전달한다.
- 첫 자동 연결로 레벨업을 적용했다. XP 저장 전후의 실제 레벨이 달라질 때만 그 순간 접속 중인 학생에게 15초 한정 축하 알림을 전달한다.
- 탐험 도감 이정표 보상도 서버 장부에 최초 지급이 확정된 경우에만 자동 축하 알림을 전달하며, 중복 보상 요청은 다시 방송하지 않는다.
- 상점 구매 방송은 모든 아이템이 아니라 황금 왕관·별 관측경·여우 친구처럼 대표 특별 아이템으로 제한하고, 원자적 구매가 확정된 뒤에만 전달한다.
- 특별 업적 방송은 도전관 만점과 누적 10회 도전으로 제한하며, 서버가 새 배지 획득 전환을 확인한 최초 한 번만 전달한다.

### 학생 캐릭터 성장/레벨업
- XP 기반 레벨업을 외형, 칭호, 해금 요소, 장식, 이동/표현 요소와 연결 가능.
- 현재 고정 200 XP 레벨업은 장기 운영 시 너무 빠를 수 있다.
- 초기에는 비교적 쉽게, 높은 레벨에서는 점차 천천히 오르는 RPG식 곡선을 추후 설계한다.
- 활동당 XP, 한 학기 예상 활동 횟수, 목표 최고 레벨을 보고 결정한다.
- 누적 XP는 보존한 채 Lv.2는 200 XP, Lv.3은 450 XP, Lv.4는 750 XP, Lv.5는 1100 XP가 필요하도록 적용했다. 다음 레벨 요구량은 200 XP부터 레벨마다 50 XP씩 증가한다.
- 성장 발자국은 Lv.5 빨강, Lv.10 주황, Lv.15 노랑, Lv.20 초록, Lv.25 파랑, Lv.30 남색, Lv.35 보라, Lv.50 별빛 순으로 변한다. 숨김 화면에서는 생성하지 않고 220ms 간격·700ms 자동 제거로 제한하며 기기의 동작 줄이기 설정을 따른다. 오로라는 자동 레벨 보상이 아니라 Lv.50 이상만 상점에서 구매할 수 있는 독립 성장 효과이며, 다른 장착 슬롯을 차지하지 않고 구매 확정 후 자동 활성화된다.
- 성장 칭호·배지는 기존 Lv.2·3·5에 이어 Lv.10 `숲길 개척자`, Lv.20 `지혜의 길잡이`, Lv.30 `별빛 연구자`, Lv.40 `마을 수호자`, Lv.50 `전설의 학습가`로 확장한다. 학생 기록 화면의 다음 성장 보상은 서버가 계산한 이 순서를 보여 주며, 기존 수수께끼 만점 업적 칭호는 별도 성취로 계속 우선 표시한다.

### 학습 활동별 도전 횟수
- 교사가 활동마다 허용 도전 횟수를 설정할 수 있게 한다.
- 수수께끼/평가형은 1회, 연습형은 여러 번 또는 제한 없음 등으로 구분한다.
- 학생별 남은 횟수, 교사 초기화/추가 허용, XP 중복 획득 방지를 함께 설계한다.

### 교과별 문제 유형
- 수학: 랜덤 출제형 반복 연습, 주관식 입력형 우선.
- 국어: 객관식 선택형 우선.
- 1회 평가형과 반복 연습형을 같은 활동 엔진 설정값으로 구분하는 방향 검토.
- 공통 문제 형식의 첫 확장으로 기존 객관식과 함께 직접 입력형(`type: input`, 복수 인정 정답)을 지원한다. 책마루 한 문제에서 실제 적용하며 공백·대소문자 정규화, Enter 제출, 태블릿 입력, 교사 수정 이력의 인정 정답 보존을 함께 검증한다.
- 관리자 문제 수정 화면에서 입력형 문제 문장과 쉼표로 구분한 인정 정답 1~8개를 편집한다. 브라우저와 서버가 빈 값·중복 정답·개수 제한을 각각 검사하고, 저장 확인에는 실제 인정 정답 목록을 보여 준다.
- 배움터의 첫 수학 활동으로 덧셈·뺄셈·구구단 5문제 랜덤 계산 연습을 제공한다. 서버가 정답을 숨긴 문제 묶음을 발급하고 직접 채점한 완료권과 일치할 때만 공통 활동 기록·XP를 저장한다. 문제 묶음·현재 번호·입력 답은 이어하기 체크포인트에 보관하고 교사 도전 횟수 설정에 연결한다.

### 교사의 학생 데이터 수정/복구
- 오류, 잘못된 기록, 중복 계정, 실수 입력에 대비해 관리자 화면에서 학생 데이터를 안전하게 수정/복구하는 기능 제공.
- 후보: 이름/계정, XP, 점수, 활동별 시도/성적, 해금/장착 상태.
- 파생값은 원본 XP/활동 기록과 일관성을 유지한다.
- 위험한 수정은 확인 절차와 가능하면 자동 백업/변경 이력/되돌리기를 제공한다.
- DB 직접 조작 없이 관리자 UI에서 처리한다.
- 첫 교정 기능으로 학생 XP 목표값 수정을 추가했다. 0~1,000,000 정수와 3자 이상의 교정 이유를 요구하고, 기존 점수 변경 장부 트리거와 최근 활동 로그에 변경 내용을 남긴다.
- 활동별 도전 횟수·최고·최근·누적 점수도 목표값으로 교정할 수 있다. 횟수와 점수의 관계를 서버가 검증하고, 도전관 수수께끼는 기존 학생 요약 기록과 함께 원자적으로 맞춘다. XP는 자동으로 바꾸지 않으며 교정 이유를 활동 로그에 남긴다.
- 꾸미기 화면이 꼬인 학생은 관리자가 기본 캐릭터·장착 슬롯만 초기화할 수 있다. 구매·보유 아이템, 별, XP, 점수는 유지하고 복구 이유를 활동 로그에 남긴다.
- 교사는 수업 보상·복구 목적으로 상점 아이템의 영구 보유권을 학생에게 지급할 수 있다. 별은 차감하지 않고 중복 지급은 무변경 처리하며, 별 장부에 0점 변동의 교사 지급 사유를 남긴다.
- 학생 이름 오타는 관리자 화면에서 수정한다. 계정과 활동·오류·점수·별 장부, 보유 아이템 호환 사본, 탐험 도감, 이어하기 기록, 추가 도전 횟수·이력을 한 트랜잭션으로 이동한다. 대상 이름이나 설정 키가 충돌하면 아무것도 바꾸지 않고, 성공하면 기존 학생 세션을 종료한다.
- 관리자 화면의 전용 교정·복구 이력판은 XP 교정, 활동 기록 교정, 꾸미기 복구, 이름 수정, 교사 아이템 지급을 시간순으로 모아 학생 이름·변경 종류·사유를 보여 준다.
- XP와 활동 기록 교정은 이력판에서 한 번 되돌릴 수 있다. 현재 값이 교정 직후 값과 정확히 같을 때만 이전 값으로 복원하고, 이후 학습이나 추가 교정으로 값이 달라졌다면 덮어쓰지 않고 거부한다. 되돌리기 자체도 이유와 함께 새 이력으로 남긴다.

### 튕김/비정상 종료 복구와 활동 체크포인트
- 이미 SQLite에 저장된 계정·점수·XP·완료 기록은 계속 보존한다.
- 진행 중 활동은 문제 번호, 현재 점수, 응답 상태 등 최소 체크포인트를 임시 저장해 `하던 활동을 이어서 할까요?` 흐름으로 복구한다.
- 체크포인트는 완료 기록/XP와 분리하고 활동 완료 시 삭제하여 중복 보상을 막는다.
- 서버 재시작 후 재로그인해도 체크포인트가 유효하면 이어갈 수 있게 설계한다.
- 수수께끼·책마루의 진행 체크포인트를 로컬 저장과 동시에 교사 PC 설정 저장소에도 학생별로 보관한다. 새 패드 로그인 시 최신 서버 체크포인트를 먼저 동기화하며, 완료 시 로컬·서버 사본을 순서대로 삭제한다.
- 생각의 숲·도전의 산도 선택된 랜덤 문제 묶음, 현재 길, 정답 수, 현재 문제의 선택지, 제출 식별자를 체크포인트에 함께 저장한다. 다른 패드에서 이어도 같은 문제 순서와 답변 상태를 복원하며, 이미 맞힌 문제의 점수는 다시 올리지 않는다.
- 관리자는 학생 활동 상세에서 진행 중인 이어하기 기록만 확인하고 개별 삭제할 수 있다. 삭제는 완료 점수·XP·활동 기록과 분리하며, 삭제 표식을 서버에 잠시 보관해 다른 패드에 남은 오래된 사본도 다음 로그인 동기화 때 제거한다.
- 관리자 화면의 `진행 중인 활동` 현황판에서 학급 전체 학생의 활동명과 마지막 저장 시각을 한눈에 확인하고, 학생 상세로 들어가지 않아도 필요한 이어하기 기록만 삭제할 수 있다.

### 오류 리포터 추가 발전
- 오류 그룹별로 `확인 중·해결됨·보류` 상태와 500자 이내 교사 메모를 저장한다. 해결 시각 이후 같은 그룹이 다시 수집되면 `해결 후 재발`로 표시한다. 상태는 오류 종류·메시지·화면·버전의 SHA-256 그룹 키에 연결하고 최근 500개로 제한한다.
- 오류 원본은 기본 90일 보관하고 관리자가 30·90·180·365일 중 선택한다. 새 오류 수집과 설정 변경 때 오래된 기록을 자동 정리하며, 오류 폭주에도 최신 2,000건을 넘기지 않는다.
- Electron 시작 실패, 서버 소켓 오류, 처리되지 않은 Electron 예외를 사용자 데이터 폴더의 200KB 제한 JSONL 로그에 남긴다. 다음 정상 실행에서 관리자 인증 API가 최근 100건만 읽고, 진단 파일의 `teacherRuntimeErrors`에 포함한다. 사용자 데이터·앱 경로와 Bearer 토큰은 기록·내보내기 양쪽에서 다시 가린다.
- 관리자 오류 패널 상단에서도 최근 교사 프로그램·서버 실행 오류 10건을 종류·메시지·시각으로 확인한다. 실행 오류만 별도로 비울 수 있으며 학생 기기 오류 기록은 유지한다.
- 교사는 오류를 직접 해석하기보다 `진단 파일 내보내기 → ChatGPT에 첨부`하는 흐름을 기본으로 한다.

### 상점 장기 발전
- 교사는 상점 전체를 열고 닫는 것과 별개로 아이템마다 `판매 중·판매 중지`를 설정한다. 판매 중지된 아이템은 새 구매만 막고, 이미 보유하거나 장착한 학생의 아이템은 유지한다.
- 아이템마다 구매 가능 최소 레벨(1~100)을 정한다. 학생 화면과 서버가 모두 레벨 조건을 검사하며, 교사 직접 지급과 이미 보유한 아이템의 장착에는 이 조건을 적용하지 않는다.
- 학생 상점은 태블릿에서 긴 목록을 쉽게 탐색하도록 `전체·모자·안경·가방·친구` 종류 필터를 제공한다. 필터를 바꿔도 판매 상태·필요 레벨·보유 여부 판단은 동일하게 유지한다.
- 교사는 수업 행사나 특별 보상을 위해 아이템에 `한정` 표시를 붙인다. 한정 표시는 학생에게 희소성을 안내하는 표식이며 판매 여부와 분리되어, 판매 중지나 기존 보유·장착 데이터에 영향을 주지 않는다.
- 한정 아이템에는 선택적으로 판매 종료 시각을 지정한다. 교사 PC의 시각을 기준으로 종료되면 학생 버튼과 서버 구매가 함께 차단되며, 종료 시각을 비우면 수동 판매 중지 전까지 계속 판매한다.
- 한정 판매 시작 시각도 선택적으로 예약한다. 시작 전에는 학생에게 `판매 예정`과 시작 시각을 보여 주고 서버가 구매를 차단한다. 시작·종료를 모두 입력하면 시작이 종료보다 빨라야 한다.
- 학생이 상점을 계속 열어 둔 경우에도 다음 예약 시작·종료 경계에서 서버 상태를 한 번 다시 읽어 화면을 자동 전환한다. 예약 타이머는 학생 기기 시계가 아니라 응답의 교사 서버 시각을 기준으로 계산한다.
- 관리자 상점에는 별 장부의 확정 구매만 집계해 아이템별 구매 학생 수와 사용된 별 합계를 보여 준다. 학생 이름은 표시하지 않으며 이 통계는 관리자 인증 API에만 포함한다.
- 아이템별 현재 보유 학생 수도 함께 보여 교사 지급·복원·구매를 모두 포함한 실제 보급 현황을 확인한다. 손상된 보유 목록은 통계에서 제외하고 다른 학생과 관리자 화면은 정상 처리한다.
- 관리자 상점도 `전체·모자·안경·가방·친구` 필터를 제공한다. 필터는 기존 카드를 숨기고 보여 주기만 하므로 저장 전 입력한 가격·레벨·예약 시각·토글 값이 유지된다.
- 관리자 상점 입력이 바뀌면 `저장하지 않은 변경`으로 표시한다. 다시 불러오기·페이지 이탈 전에 확인하고, 저장 중에는 모든 상점 입력을 잠가 성공 응답이 오기 전 추가 수정이 섞이지 않게 한다.
- 관리자는 현재 필터에 보이는 종류의 `판매 중` 체크만 한 번에 켜거나 끌 수 있다. 숨겨진 종류와 가격·레벨·한정·예약 값은 바꾸지 않으며, 일괄 변경도 즉시 전송하지 않고 최종 저장 확인을 거쳐 반영한다.
- 향후 학습 보상 포인트, 아이템 구매, 한정 장식, 교사 지급 보상 등으로 확장 가능.
- 구매 요청은 학생 기기에서 교사 PC 서버로 보내고 서버가 실제 잔액/조건을 검증한 뒤 SQLite에 확정 저장한다.
- 학습보다 아이템 경쟁이 중심이 되지 않도록 교육적 보상 구조를 우선한다.

## 앞으로 기능 추가 원칙
1. 기존 기능을 지우기보다 누적 확장한다.
2. 이전 정상 기능을 깨뜨리지 않는다.
3. 건물은 가능한 한 실제 학습/성장 기능으로 연결한다.
4. 새 활동은 공통 학생 계정, 활동 기록, XP에 연결한다.
5. PC와 태블릿/모바일을 함께 고려한다.
6. 서버/DB 복잡성을 교사에게 노출하지 않는다.
7. 외부 인터넷 없이 내부망 핵심 기능이 동작해야 한다.
8. 앱 업데이트와 학생 데이터를 분리한다.
9. GitHub `main`이 구현 상태의 최종 기준이다.
10. 중요한 결정은 코드와 이 문서를 함께 갱신한다.
11. 구현하지 않은 아이디어도 백로그에 기록한다.
12. 1.0이라는 숫자보다 실제 교실 안정성과 데이터 안전을 우선한다.
13. 학생에게 기술적인 오류 처리 절차를 직접 시키지 않는다.
14. 오류 진단은 ChatGPT에 전달하기 좋은 형태로 자동 수집/내보낸다.
15. 정확성이 중요한 실시간 상태는 임시 추정으로 만들지 않고 필요한 서버 구조를 갖춘 뒤 구현한다.
16. 점수/별/재화 같은 실제 보상은 서버 검증 후 확정한다.
17. 실시간 기능은 교육적 가치가 있는 이벤트 위주로 제한하고 통신량을 작게 유지한다.
18. 반복 조회와 타이머는 화면이 보이거나 실제로 필요할 때만 동작하도록 설계한다.
19. 서버 응답을 기다리는 핵심 로그인/복구 흐름에는 timeout을 두어 학생 화면 무한 대기를 막는다.
