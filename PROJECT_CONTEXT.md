# Studyvillage Project Context

> 이 문서는 새 ChatGPT 대화에서도 프로젝트의 방향과 중요한 결정을 빠르게 복구하기 위한 연속성 기록이다.
> 코드의 실제 현재 상태는 항상 `main` 브랜치를 우선하며, 이 문서는 코드에 드러나지 않는 설계 의도와 운영 원칙을 보완한다.

## 기준점

- 프로젝트: **우리 학습마을 (Studyvillage)**
- 기준 버전: **v0.8.7**
- 기준 브랜치: `main`
- 다음 개발은 **v0.8.7을 훼손하거나 옛 버전으로 되돌리지 않고 누적해서 진행**한다.

## 프로젝트 목표

Studyvillage는 초등 교실에서 학생들이 태블릿/브라우저로 접속해 캐릭터로 마을을 돌아다니고, 건물별 학습 활동과 미니게임을 수행하며, 그 결과가 학생 개인 기록과 공통 성장/보상 체계에 연결되는 **교실용 학습 RPG**이다.

게임 자체가 목적이 아니라 **학습 활동을 재미있는 마을 탐험 구조 안에 넣는 것**이 핵심이다.

## 가장 중요한 운영 결정

### 1. 교실 PC가 서버

최종 기본 운영은 클라우드 우선이 아니다.

- **선생님 교실 컴퓨터 = 서버**
- 학생은 같은 학교/교실 네트워크에서 QR로 접속
- 학생 기록은 선생님 PC에 저장
- 기본 서버는 Node/Express 계열 + SQLite
- 가정 접속을 기본 목표로 하지 않는다.
- Firebase Authentication/Firestore 어댑터를 검토하고 데이터 서비스 계층도 분리했지만, 이후 결정에서 Firebase는 **주 운영 방식이 아니라 선택 가능한 보조/대체 경로**로 내렸다.

### 2. 독립 실행형 교사용 앱

교사가 개발환경을 몰라도 사용할 수 있어야 한다.

목표 흐름:

1. `Studyvillage.exe` 실행
2. 교실 서버 자동 시작
3. 접속 QR 표시
4. 학생이 태블릿으로 QR 스캔
5. 이름 + 비밀번호로 로그인
6. 학생 활동 기록은 교사용 PC의 SQLite DB에 저장

Electron 기반 교사용 실행기와 Windows portable EXE 패키징 기반을 유지한다. Node가 설치된 개발 환경에서는 기존 배치 실행 방식도 사용할 수 있다.

Electron 패키지 자체는 읽기 전용이 될 수 있으므로 실제 SQLite DB는 사용자 데이터 영역의 별도 `data/studyvillage.db`에 저장해 **앱 버전 교체와 학생 데이터 수명을 분리**한다.

### 3. 학생 데이터는 버전 업데이트와 분리

- 새 버전 배포 때문에 학생 기록이 초기화되면 안 된다.
- 관리자 백업/복원 구조를 유지한다.
- 학생 계정 인증 정보와 활동 기록은 교사용 데이터로 취급한다.

## 초기 플레이 구조에서 확정된 것

### v0.2.x

- 브라우저 시작 화면 → 마을 광장 → 학생 캐릭터 이동
- 키보드 방향키/WASD 지원
- 모바일 방향 버튼 지원
- 맵 가장자리 및 건물/나무/연못 충돌 처리
- 대각선 이동 유지
- 안내 NPC **마을지기**
- NPC 가까이에서 Space 또는 모바일 대화 버튼으로 상호작용
- 대화 중 캐릭터 이동 잠금
- 도전형 미니게임의 초기 형태로 수수께끼 퀴즈 구현
- 문제별 정답 피드백/설명, 점수, 최종 결과 및 다시하기 구조

이 구조들은 이후 기능을 추가할 때 깨뜨리지 않는 기본 플레이 경험이다.

## 데이터/서버 설계에서 확정된 것

### v0.3.x 이후

- 게임 화면과 데이터 저장 구현을 강하게 결합하지 않고 **데이터 서비스 계층을 분리**하는 방향을 잡았다.
- 클라우드 인증/Firestore 방식도 연결 가능하게 설계했지만, 실제 교실 운영의 기본 선택은 로컬 서버 + SQLite로 변경했다.
- 학생 식별/로그인과 활동 기록은 이후 XP 및 활동별 기록의 기반이 된다.

## v0.5.x 독립 실행 기반

README에 남아 있는 핵심 구조:

- Electron 교사용 실행기
- 앱 실행 시 Express + SQLite 서버 자동 시작
- 학생 이름 + 비밀번호 로그인
- 학생 기록 SQLite 저장
- 교사용 관리자 인증 및 학생 계정 관리
- 유선/Wi-Fi 어댑터별 QR 자동 생성
- 관리자 백업/복원
- Windows portable EXE 빌드 설정
- GitHub Actions Windows 자동 빌드 기반

이 부분은 이후 RPG 기능이 커져도 운영 기반으로 계속 유지한다.

## v0.8.x 마을 확장 방향

### v0.8.0 — 큰 마을/카메라 기반

- 한 화면에 모든 것을 억지로 넣는 방식에서 벗어나 **큰 마을을 이동하는 구조**로 확장
- 스크롤 카메라 기반
- 넓어진 마을 캔버스에서 캐릭터 이동에 따라 화면이 따라가는 구조

### v0.8.1 — 마을 구역화

- 마을을 구역 단위로 구성
- 도로와 중앙광장 추가
- 건물과 학습 기능을 앞으로 구역별로 확장할 수 있는 기반

### v0.8.2 — 건물 내부

- 건물을 단순 배경 오브젝트로 두지 않고 **실제로 들어갈 수 있는 장소**로 전환
- 건물 입구/내부 전환 시스템
- 내부 화면과 전환 UI
- 이후 건물별 학습 활동은 이 구조 위에 연결한다.

### v0.8.3 — 책마루 어휘 활동

- **책마루** 내부와 어휘 미니게임 연결
- 건물에 들어가는 행위가 실제 학습 활동으로 이어지는 첫 본격 사례
- 어휘 게임 UI 및 로딩/연결 구조 구현

### v0.8.4 — 활동 기록 + 공통 XP

- 개별 학습 활동 결과를 활동별 기록으로 저장
- 책마루 어휘 활동 결과도 기록에 연결
- 활동 보상을 **공통 XP 체계**에 연결
- 앞으로 새로운 건물/미니게임을 추가할 때 각 게임마다 별개의 성장 체계를 만드는 대신, 가능한 한 동일한 학생 기록/XP 기반에 연결한다.

### v0.8.5 — 내 기록의 활동별 성적표

- 학생의 `내 기록` 화면에 활동별 기록 카드 영역 추가
- 활동별로 도전 횟수, 최고점수, 최근점수, 누적점수를 확인할 수 있게 함
- 새 활동이 `activity_records`에 추가되면 카드가 자동으로 늘어나는 구조
- 모바일 화면에서는 활동 통계를 2열로 보이도록 구성

### v0.8.6 — 도전관 수수께끼 기록 연결

- 기존 도전관 수수께끼는 오래된 전체 기록 필드(`attempts`, `bestScore`, `lastScore`, `totalScore`)를 사용하고 있어 `activity_records`에 별도 행이 없었다.
- 0.8.5 활동별 기록 화면에는 이미 `riddle` 카드 정의가 있었지만 실제 데이터가 비어 보일 수 있었다.
- **XP 중복 지급을 피하기 위해 수수께끼 결과를 별도 activity API에 다시 저장하지 않고**, 기존 수수께끼 전체 기록을 `riddle` 활동 카드로 읽어오는 호환 브리지를 추가했다.
- 따라서 기존 학생의 과거 수수께끼 기록도 그대로 보이고, 기존 XP/보상 계산은 변경하지 않는다.
- 향후 기록 구조를 완전히 통합할 때는 데이터 마이그레이션 여부를 별도 논의한다.

### v0.8.7 — 교사용 학생별 활동 기록

- 교사용 관리자 화면의 학생별 행에 **활동 보기** 기능 추가
- 학생을 선택하면 레벨, XP, 칭호, 배지 수와 함께 활동별 도전 횟수/최고점수/최근점수/누적점수를 확인할 수 있다.
- 책마루 어휘 활동은 `activity_records`의 실제 기록을 사용한다.
- 도전관 수수께끼는 v0.8.6과 동일하게 기존 전체 수수께끼 기록을 `riddle` 카드로 보여주는 호환 방식을 사용한다.
- 교사용 확인 기능은 조회만 추가하고, 학생 XP나 활동 기록 계산 방식은 변경하지 않는다.
- 모바일 관리자 화면에서도 활동별 카드를 한 열로 확인할 수 있게 한다.

## 아이디어/보류 백로그 — 지금 당장 구현하지 않아도 잊지 말 것

이 항목은 개발 도중 사용자가 떠올린 아이디어 중 **지금 버전에 바로 넣으면 구조나 밸런스가 흔들릴 수 있어 1.0 완성 후 차근차근 논의·반영할 내용**을 기록한다.

### 학생 캐릭터 성장/레벨업

- 학생 캐릭터가 학습 활동과 XP를 통해 **레벨업하는 구조**를 발전시키고 싶다.
- 단순 숫자 레벨뿐 아니라 캐릭터의 성장 체감이 생기도록 외형, 칭호, 해금 요소, 장식, 이동/표현 요소 등과 연결할 가능성을 열어둔다.
- 구체적인 보상 방식은 1.0 이후 사용자와 함께 논의한 뒤 확정한다.

### 경험치/레벨 곡선 재설계 필요

- 현재 대화에서 한 차례 **100 XP마다 1레벨** 같은 단순 기준을 이야기했으나, 이 방식은 장기 운영 시 레벨이 너무 빨리 오를 가능성이 있다.
- RPG처럼 **초기 레벨은 비교적 쉽게 오르고, 레벨이 높아질수록 다음 레벨까지 필요한 XP가 점차 증가하는 성장 곡선**을 고려한다.
- 아직 공식 수식/테이블은 확정하지 않는다.
- 1.0 완성 후 실제 활동당 XP 지급량, 한 학기 예상 활동 횟수, 목표 최고 레벨 등을 함께 보고 밸런스를 설계한다.
- 중간 개발 단계에서 임의로 XP 구조를 크게 바꾸지 않는다. 현재는 기록과 보상 연결 기반을 우선 안정화한다.

### 운영 원칙: 아이디어를 구현보다 먼저 잃지 않기

- 사용자가 개발 중간에 "나중에 이런 것도 하고 싶다", "이건 조금 바꾸고 싶다", "지금 바꾸면 복잡하니 나중에 하자"라고 말한 사항은 **코드에 즉시 반영하지 않더라도 이 문서의 백로그에 기록**한다.
- 1.0까지는 핵심 루프와 안정성을 먼저 완성한다.
- 1.0 이후 백로그를 사용자와 하나씩 검토하면서 실제 반영 여부와 우선순위를 결정한다.
- 아이디어가 기록되었다고 해서 자동으로 확정 기능이 되는 것은 아니다. **보류 아이디어와 확정 설계는 구분**한다.

## 앞으로 기능을 추가할 때 지킬 원칙

1. **기존 기능을 지우고 새로 만드는 방식보다 누적 확장**을 우선한다.
2. 버전 하나를 올릴 때 기존 버전의 정상 기능이 깨지지 않도록 한다.
3. 건물은 장식이 아니라 가능한 한 **학습 활동으로 연결되는 공간**으로 발전시킨다.
4. 새로운 활동은 가능하면 공통 학생 계정, 활동 기록, XP 체계에 연결한다.
5. PC 키보드뿐 아니라 학생 태블릿/모바일 조작도 계속 고려한다.
6. 교사가 실제 교실에서 쉽게 실행할 수 있어야 하므로 서버/DB 운영 복잡성을 교사에게 노출하지 않는다.
7. 인터넷/외부 클라우드가 없어도 교실 내부망에서 핵심 기능이 동작하는 방향을 기본으로 한다.
8. 앱 업데이트와 학생 데이터는 분리한다.
9. GitHub `main`의 최신 코드가 구현 상태의 최종 기준이다. 대화 기억과 코드가 충돌하면 코드를 먼저 확인하고, 설계 의도는 이 문서와 이전 대화를 함께 참고한다.
10. 새 버전에서 중요한 설계 결정이 생기면 **코드만 수정하지 말고 이 문서도 함께 갱신**한다.
11. 구현하지 않은 아이디어도 잊지 않도록 **아이디어/보류 백로그**에 기록한다.

## 새 채팅에서의 권장 복구 절차

새 대화에서 Studyvillage를 이어갈 때 매번 과거 전체 대화를 다시 읽을 필요가 없도록 다음 순서로 시작한다.

1. `PROJECT_CONTEXT.md` 확인
2. GitHub `main` 최신 커밋/버전 확인
3. 필요할 때만 이전 대화의 특정 결정 검색
4. 최신 버전부터 누적 개발
5. 중요한 새 결정이나 보류 아이디어는 다시 `PROJECT_CONTEXT.md`에 기록

## 현재 이어갈 지점

**v0.8.7 완료 상태에서 다음 버전(v0.8.8) 설계를 시작한다.**

현재까지의 핵심 축은 다음과 같다.

`교실 PC 서버/SQLite` → `학생 로그인/기록` → `독립 실행형 교사용 앱` → `RPG 마을 이동` → `큰 마을/구역` → `건물 내부` → `책마루 어휘 활동` → `활동별 기록 + 공통 XP` → `내 기록 활동별 카드` → `도전관 수수께끼 기록 호환 연결` → `교사용 학생별 활동 기록`